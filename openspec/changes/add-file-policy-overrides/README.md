# add-file-policy-overrides

为 semantic config v2 增加显式、可解释且只作用于 capability check settings 的文件级覆盖规则

## 当前状态

这是临时且未审计的 OpenSpec change。`tasks.md` 的 1.1 仍未完成；它完成前不得实现或把本方案视为已批准。

## 阅读顺序

1. `tasks.md` 1.1；2. `proposal.md`；3. `design.md` 的 numbered decisions；4. `specs/**/spec.md`。

## 直接依赖

- `introduce-content-quality-foundation`
