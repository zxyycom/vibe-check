# stabilize-scan-input-path-and-ignore-semantics

稳定 explicit changed-files 列表路径、读取错误分类，以及 current/baseline Git collection
的 success/fallback 边界。

范围与影响见 `proposal.md`，实现理由见 `design.md`，可观察合同见 `specs/`，交付顺序
与验证入口见 `tasks.md`。
