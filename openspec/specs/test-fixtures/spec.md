# test-fixtures Specification

## Purpose
TBD - created by archiving change add-simulated-project-fixtures. Update Purpose after archive.
## Requirements
### Requirement: Checked-in project fixture suite
The repository SHALL provide deterministic checked-in project fixtures as CLI scan test environments. The first suite MUST include fixture environments that cover TypeScript `.ts`, Go `.go`, Rust `.rs`, and Python `.py`, and each fixture source/configuration/ignore file MUST be hand-written, checked into the repository, and runnable by tests without network access or language package installation. Normal single-language fixture environments MUST NOT intentionally mix other programming languages. JavaScript, JSX, TSX, and other language fixtures MUST NOT be included as first-suite supported fixture projects; they MAY appear only in a dedicated mixed fixture as unsupported ordinary-file inputs when a test is proving unsupported classification.

#### Scenario: First language fixtures exist
- **WHEN** a reviewer inspects the checked-in project fixture suite
- **THEN** the suite contains project fixtures covering `.ts`, `.go`, `.rs`, and `.py` source files as supported inputs
- **AND** the fixtures do not require external dependency installation before `vibe-check scan` can read them
- **AND** the fixture source files are checked-in test environment inputs rather than generated by test code or build scripts

#### Scenario: Later language inputs are isolated to mixed fixture
- **WHEN** a reviewer inspects the first checked-in project fixture suite
- **THEN** normal single-language fixture environments do not contain JavaScript, JSX, TSX, or other non-first-suite programming language inputs
- **AND** any such files that appear for boundary testing are documented as unsupported ordinary-file inputs that must not produce supported language metrics

#### Scenario: Fixture projects are scanned in place
- **WHEN** CLI contract tests scan a checked-in project fixture
- **THEN** tests pass the checked-in fixture project path directly to `vibe-check scan`
- **AND** repeated scans use the same source layout and prove the same documented proof targets

#### Scenario: Fixture-backed tests are read-only
- **WHEN** fixture-backed tests run
- **THEN** they pass checked-in fixture paths directly to `vibe-check scan`
- **AND** they MUST NOT create, copy, append, rewrite, synthesize, or generate source/configuration/ignore files used as scan input
- **AND** parallel fixture-backed tests rely on read-only scan behavior

### Requirement: Fixture coverage boundaries
The checked-in project fixture suite SHALL cover project-shape boundaries that affect scan scope and metrics behavior. The suite MUST include VCS ignore input, default excluded directory input, unsupported ordinary files, generated/vendor/cache boundaries, and at least one multi-language fixture.

#### Scenario: Scope boundary fixture covers exclusions
- **WHEN** the fixture suite is used by scan scope tests
- **THEN** at least one fixture contains files under default excluded directories such as `target`, `.venv`, `generated`, `vendor`, or cache paths
- **AND** proof targets verify those paths are treated as excluded scan inputs

#### Scenario: Mixed fixture covers unsupported files
- **WHEN** the fixture suite is used by CLI contract tests
- **THEN** at least one fixture contains unsupported ordinary files
- **AND** proof targets verify unsupported files do not produce supported language metrics

### Requirement: Fixture proof targets
Each checked-in project fixture environment SHALL have documented proof targets used by tests. Fixture environments MAY contain multiple file-level or function-level test cases. Proof targets MUST focus on owner-defined behavior such as JSON schema validation, file collection/classification, measured language presence or absence, warning/gate status, and diagnostics status; fixtures MUST NOT be treated as a separate source of product semantics, human-readable rendering behavior, or exact metric numbers.

#### Scenario: Tests assert owner-defined behavior
- **WHEN** a CLI contract test scans a checked-in project fixture
- **THEN** the test asserts documented proof targets that trace back to CLI, scan scope, quality metrics, or JSON schema/output-contract behavior

#### Scenario: Tests avoid full report snapshots
- **WHEN** a fixture scan produces JSON output
- **THEN** tests validate the JSON schema and selected stable proof targets instead of comparing a full JSON snapshot unrelated to the owner contract

#### Scenario: Tests avoid fixture-owned metric snapshots
- **WHEN** a fixture scan produces metrics
- **THEN** tests do not require checked-in fixture metadata to fix exact LOC totals, scope counts, or supported counts unless a count is derived from documented file classification solely to observe a scan-scope boundary

### Requirement: Warning threshold fixture support
The checked-in project fixture suite SHALL support testing `file.too_many_lines` warning and gate behavior with a hand-written checked-in supported source file. Tests MUST use the checked-in source file as scan input, and generated test-only files MUST NOT be used as scan input. The threshold source MAY live in a suitable existing fixture environment or in a dedicated threshold fixture environment.

#### Scenario: Blocking threshold source is checked in
- **WHEN** a test needs to verify the blocking `file.too_many_lines` branch
- **THEN** the fixture suite contains a hand-written checked-in supported source file with at least 800 total lines
- **AND** the resulting scan verifies a blocking warning and failed gate

#### Scenario: Threshold input is not hidden in test logic
- **WHEN** a reviewer inspects the fixture-backed threshold test
- **THEN** the warning-triggering source content is visible in the fixture project
- **AND** no test branch generates source content solely to reach the warning limit

### Requirement: Fixture maintenance documentation
Checked-in project fixtures SHALL be documented in the test maintenance materials. Adding, removing, or changing a fixture that affects stable tests MUST update the relevant test case ledger or `@case` mapping according to the repository testing workflow.

#### Scenario: Fixture-backed tests are traceable
- **WHEN** a fixture-backed CLI contract test is added or changed
- **THEN** the corresponding test case entry or `@case` marker identifies the proof target and fixture responsibility
