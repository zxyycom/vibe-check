interface:
  display_name: "长期决策记录"
  short_description: "主动恢复、审阅并维护可回放、可验证且可演进的长期决策"
  default_prompt: "使用 $decision-records 在形成长期决定或处理冲突前恢复相关活动判断，并结合当前事实逐条判断相关决策应遵守当前基线、作为未来方向输入还是明确实施；只把具有长期价值且完整到可审核的方向写成 candidate，审核通过后再选择对齐状态并激活；发现只有部分内容对齐时先判断记录是否过粗，需要时用闭合拆分建立可独立演进和对齐的后继；维护前读取领域契约并用 CLI 验证。"
