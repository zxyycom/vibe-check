interface:
  display_name: "长期决策记录"
  short_description: "主动恢复、审阅并维护可回放、可审计且可演进的长期决策"
  default_prompt: "使用 $decision-records 在形成长期决定或处理冲突前恢复相关活动判断，区分已对齐基线、未对齐方向输入、方向实施、一次性例外、长期修订或一致性问题；只维护已确认且值得长期回放的判断，写入前读取领域契约并用 CLI 验证。"
