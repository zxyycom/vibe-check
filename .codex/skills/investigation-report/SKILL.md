---
name: investigation-report
description: >-
  在当前请求或生效项目规则明确要求记录、沉淀、创建、更新或审阅调查报告时使用。
  以一份可独立复核的报告保存一轮形成时的背景、依据、结果和边界，
  并用稳定 Investigation ID、tags 和直接前序关系维护分类与认识演进。
metadata:
  version: "56"
---

# Investigation Report

## 目标与适用范围

用一份报告保存**一轮调查在形成时的认识**，使未参与原对话的读者仍能复核：为什么调查、实际检查了什么、依据是什么、得出了什么，以及结论的适用边界。

当前请求或生效项目规则明确要求记录或维护调查报告时使用，适用于各领域；其他调查在当前任务中交付。领域方法由相应任务承接，当前事实、长期方向、实施任务与稳定测试义务各归其 owner。

正文解释关键认识，随附资源按需补充现场与做法，派生索引支持查找和追溯。可独立复核，是指读者能据此审查结论如何形成及其适用边界；结论可信度取决于来源、方法与推理，而非附件是否齐全。

## 自行审查与授权

agent 在当前请求或生效项目规则授权的范围内，自行审查证据、报告边界、资源、关系与发布条件，并执行维护。机械 readiness 与 preflight 只证明相应准备情况，语义审查由 agent 另行完成。

只有关键事实或取舍无法从现有材料确定、超出委托范围，或当前指令明确要求人工确认时才询问。删除等高风险操作仍须有覆盖精确目标与影响的授权。

## 读取路径

1. 先读目标工作区指令，按当前问题定位事实来源。开始或继续调查工作前，先按[查找与阅读](#查找与阅读)恢复相关已有报告，再决定复用、完善、新建或继续调查。
2. 创建、修改、publish、剔除或结构审阅前，完整读取[固定契约](references/investigation-report-contract.md)。它承接身份、正文结构、关系、资源、索引和维护不变量；索引精确机器结构由相邻 [Schema](references/investigation-index.schema.json) 承接，命令参数查 `help <command>`。
3. 出现 CLI 诊断、锁、权限或中断写入等恢复条件时，读取[维护恢复](references/maintenance-recovery.md)，只对诊断声明的范围判断结果。

## 工作流程

### 1. 先恢复已有调查认识

开始新一轮调查或建立新报告前，先按[查找与阅读](#查找与阅读)选择信息损失最小的入口恢复可能相关的已有报告：已知准确 selector 直接 `show`，metadata 条件用 `list`，主题或正文措辞用 `search`，需要理解认识演进时 `trace`。查询在足以区分复用、同轮完善、独立新轮次或无需记录时停止，不要求无差别扫描；准备新建时，再确认没有相关 candidate 与正式报告已经覆盖同一问题。

按下表分流。询问复用还是复查是减少重复调查的体验优化，不是正确性门禁：

| 当前情形 | 下一步 |
| --- | --- |
| 已有报告大致回答同一问题，用户未说明是否需要当前验证 | 先概述已有认识，再询问复用既有结论还是按当前事实复查。 |
| 用户只要求查找、总结或审阅已有材料 | 直接交付已有结果及其形成时边界。 |
| 用户明确要求重新调查、最新结果或当前事实，或项目规则要求当次调查 | 以旧报告为背景，直接按当前事实复查。 |
| 没有足够相关的已有报告 | 继续正常调查。 |

概述已有认识时说明命中报告的身份、形成时间、原问题、关键结果、主要依据、适用范围、未覆盖内容和剩余未知，并明确它保存的是形成时认识，尚未证明当前事实。用户选择复查后，以旧报告为输入按当前事实取证，再按“确定本轮报告与维护方式”判断同轮完善还是独立新轮次。

### 2. 确定本轮报告与维护方式

先按[报告边界与有效演进](references/investigation-report-contract.md#报告边界与有效演进)区分同轮完善与独立新报告，再判断真实直接前序。分篇依据是独立轮次的保留价值，而非新发现、纠错次数或 Git 提交。

| 当前需要 | 处理 |
| --- | --- |
| 判断已有报告是否可复核 | 只读审阅正式报告；未建立内容先审阅 candidate。 |
| 同一轮调查补充材料、纠正误述或继续收敛认识 | 完善原报告或候选，准确标明依据及其实际时点。 |
| 形成需要独立保留的新一轮认识 | 创建完整新报告；再单独判断是否存在真实直接前序，关系可以为空。 |
| 既有报告存在记录、格式或链接错误 | 原地修正；已提交不改变这一选择。 |
| 更正稳定 ID/name | 使用 `rename` 统一维护身份、关系、位置和资源引用。 |
| 明确剔除正式报告或候选 | 统一用 `discard`，按稳定 ID 自动识别目标种类，按契约确认删除范围。 |

需要新建时，用 `new` 创建集合外的 candidate，再完成正文、资源与关系：

- 当前发生的调查：传入不含日期前缀的 name，省略 `--formed-at`，由工具一次生成当前 UTC 时间并派生完整 ID。
- 已知形成时间或补录历史调查：显式提供 `--formed-at`；ID 日期仍须与该时间的 UTC 日一致。

后续编辑和预检均围绕已创建的候选继续。

### 3. 形成可独立复核的认识

按当前任务适用的方法取得证据，将一份报告限定为一轮能够独立汇报的认识。四项固定核心共同回答本轮问题：

| 核心内容 | 必须让读者恢复的内容 |
| --- | --- |
| 形成时背景 | 当时发生了什么，以及促成调查的已知事实、假设、未知和约束。 |
| 调查目的 | 本轮具体问题、准备支持的判断和预定边界。 |
| 调查范围与依据 | 实际检查对象、来源、时点或版本、方法、覆盖范围和未覆盖内容。 |
| 调查结果与边界 | 已确认事实、推断、建议、实际动作和未知；适用条件、不可外推范围与重新调查条件。 |

正文围绕关键认识组织，按判断需要补足条件：因果解释说明观察与预期、触发条件、候选解释及排除依据；计量说明来源、样本、指标、窗口与误差；方案比较说明比较条件、取舍、授权、恢复与验证边界。相关性、因果判断、建议与实际结果按各自证据强度表达。这些需要可以组合，具体写法随问题调整。

渐进探索保留使判断改变的转折：初始问题 → 实际动作与观察 → 解释的保留或修正 → 当前认识与剩余未知。依据标明实际取得的时点；尚未闭合的环节保留为阶段性结论，按“确定本轮报告与维护方式”判断同轮完善或独立成篇。

例如，性能调查从“接口变慢”缩到“重复读取”，再定位缓存失效 Bug，正文解释每次收窄依赖的观察；详细测量和诊断代码按需随附。

### 4. 选择随附资源

正文应让读者独立理解关键认识、依据与边界；资源用于进一步了解细节和当时做法。按以下顺序取舍：

1. **判断是否保留**：材料能补充哪项重要条件、观察或动作？短而关键的内容放正文，稳定来源足够时直接引用，需要随报告保存的细节再附资源，其余省略。
2. **确定保留范围**：以理解关键现场与逻辑为准。长日志、数据或输出可以节选、去重、汇总、脱敏和简化，保留影响判断的状态、顺序、差异及反例，并简要标明处理方式。
3. **选择表达形式**：优先采用可直接阅读、便于 Git 比较的等价纯文本、表格或代码。文本会丢失影响判断的信息时，才保留必要的最小非文本材料，并说明用途。
4. **说明阅读用途**：正文交代资源的来源、条件、关键内容及何时需要读取；资源的详略以此为准。

调查中使用的一次性分析代码、测试代码或查询，可以原样或节选保留，用于直接解释当时的动作与逻辑。代码保持真实语言形式，节选说明省略范围；纯伪代码用 `.txt` 等明确的文本形式区分。资源默认供阅读，复现准备、重跑验证和工具维护仅按任务要求承担；执行及其他副作用仍须相应授权。

资源统一以 `./_resources/<resource-id>` 链接，预置于最终 owner 路径，也可共享既有正式 owner 的资源。publish 保持链接和位置，资源按实际交付范围另行暂存。

新增实质材料另存为新资源，同轮完善归原报告，独立轮次归新报告。已保留材料的原地修改限于准确恢复、格式修复或移除敏感信息；旧样本与其结论对应的历史须保真。秘密和认证材料不得保存。

### 5. 维护分类与直接关系

- **分类**：tags 表达有正文依据的可检索主题。
- **认识演进**：无真实直接前序时使用空关系；存在直接前序时，按变化选择 `补充`、`复查`、`修正`、`推翻`、`归并` 或 `拆分`。关系由后继指向前序，合法语义与图形见固定契约。
- **关系摘要**：新建或调整真实直接关系时，依据两端正文为每条边填写 `summary`，简述相对前序的具体补充、复核内容或变化；格式与示例见[固定契约](references/investigation-report-contract.md)。
- **关系维护**：candidate 在起草时声明完整关系，已建立报告用 `set-relations` 事务替换完整关系集合。发布同批互相关联候选时，按契约核对最终关系图闭合。

### 6. 审核并建立候选

1. 用 `show-candidate` 核对 `scaffoldValid`、`bodyReady` 与 `resourceReady`；它们分别表达结构、正文和资源的机械准备情况。
2. 自行审查完整内容，确认正文可信、关系真实、资源必要且安全，并核对当前任务的 publish 授权。
3. 用 `publish <selector...> --preflight` 预演显式候选的最终集合，或用 `set-relations --preflight` 预演完整替换；两者都返回预计的完整 `relationReview`、零写入且不保存确认凭据。正式命令仍会重新读取基线、候选和资源并完整验证。
4. 授权和审核均满足后 publish。若存在手工正式来源变化，先按下一节同步，再发布候选。

正式根目录的完整报告一旦写入即已建立。候选通过 `publish` 正常建立；手工正式来源变化按下一节用 `sync-index` 接纳。

### 7. 同步、验证与交付

1. 只读审阅运行适用的 `check` 或 `publish --preflight`，保持集合状态不变。
2. 一批手工正式报告编辑可先共同完成，期间用 scoped check 获取局部反馈。在索引查询、已有关系事务、正式删除、默认全量检查、暂存或交付需要当前集合前，统一运行一次 `sync-index`。它默认写入并发布完整有效索引，`--preflight` 零写入预演同一验证；`--select ...` 只接纳所选 ID 的已知来源变化，仍发布完整索引，合法 candidates 不参与同步。已知合法来源变化的规范顺序是先 `sync-index` 再默认全量 `check`；未解释的索引异常先 `check` 诊断，再同步或修复。
3. `set-relations` 与正式 `discard` 要求当前索引，并在成功事务中同步索引。只改资源字节时保留索引；暂停、失败或 cleanup 诊断按契约与恢复手册处理。
4. publish、同步或事务完成后运行默认全量 `check`，并按下列完成标准复核实际结果。
5. 需要 Git pending 快照时，同步和全量检查后用 `stage <investigation-id...> [--scope all|index|domain]` 选择正式报告。默认 `all` 原子写入所选索引投影、所选正式报告 Markdown 及其完整 owner 资源树；`index` 只写入索引投影；`domain` 只写入报告与 owner 资源并保持 pending 索引字节不变。工作区索引陈旧或无效时零写入停止，按诊断同步后再重试。基线-only 旧 ID 写入删除，重命名显式同时选择旧 ID 与新 ID；owner 资源成员取工作区与 `HEAD` 的路径并集，其他 owner 的资源保持不变。pending、commit 与 push 仍由调用方按授权显式完成。

## 完成标准

1. 正文独立说明背景、问题、关键依据、结果与边界；重要探索转折和阶段性未知清楚，主张强度与证据一致。
2. tags、直接前序关系和资源各有正文依据；候选在 publish 前具备完整 readiness、preflight、语义审核和授权依据。
3. 资源各有必要的阅读用途，保留范围与表示形式适合 Git 维护，节选和简化的性质清楚；复现要求按任务单独验证。
4. 只读审阅说明可确认范围、问题与未知；写入交付说明实际变化、同步和全量检查结果、语义审查结论及未证明的边界。

## 查找与阅读

| 已知信息或阅读目的 | 入口 |
| --- | --- |
| 准确 Investigation ID 或唯一 name | `show <selector>` |
| tags、形成时间、关系类型或直接关系目标 | `list`；有关系条件时读取返回的命中边依据，普通列表按需筛选、翻页或用 `--detail` 展开记录概览 |
| 主题、概念、原因或正文措辞 | `search <text>`；文本命中与关系筛选依据分别读取，再用完整 ID 继续读取 |
| 在受限索引切片中追溯认识演进 | `trace <selector>` |
| 审核尚未建立的候选 | `candidates` → `show-candidate <selector>` |

Investigation ID 是 frontmatter `id` 声明的稳定身份，`sourcePath` 只定位文件。查询后使用返回的完整 ID 继续操作。

`list` 与 `search` 只查正式报告，候选通过独立入口读取。关系筛选依据、文本证据的分工与 `filterRelations` 的读取边界由[索引与查询](references/investigation-report-contract.md#索引与查询)承接；完整正文和完整直接关系继续用 `show` 读取。默认搜索读取报告 Markdown，`--in metadata` 只反映已发布索引快照。持久索引陈旧时，`list`、`trace` 与 metadata `search` 仍返回最后一次发布快照并给出 warning；`show` 验证当前文件身份后返回当前正文；默认内容搜索基于已验证的当前来源投影。这些 warning 标明数据源与 `sync-index` 恢复命令，快照结果不能支持对当前全集的否定性结论。遇到降级或截断 warning 时，先按该节确认来源与结果边界，再据此下结论。

所有正式报告保留在同一集合，直接关系描述认识演进。`trace` 只读一次当前受检索引：默认向 stdout 输出稳定终端关系图，而不是旧的平铺文本或 Mermaid；`--json` 才输出同一份 trace 查询成功结果的稳定 JSON envelope，因而不改变 `anchorId`、实际 `direction`、实际 `limits` 或成员边界。文本图用 `L0/L1/...` 表示稳定图层，`* trace` 标记实际遍历成员，`~ context` 标记为完整拆分或纯归并事件补齐的成员，并在存在时呈现 relation summary、frontier 与 blocked event。无限深度在 JSON 的 `limits.depth` 中表示为 `"all"`。`traceIds` 是实际沿请求方向到达、可继续扩展的成员，`contextIds` 只为完整拆分或纯归并事件闭合而加入，不递归扩展；两者互斥且并集恰为 `entries` 的键。省略参数时使用 `direction=both`、`depth=5`、`max-records=50`；`--depth all` 取消深度限制。用 `coverage`、`frontier` 和（存在时）`blockedEvent` 判断结果是否完整：frontier 是下一次查询可用的 anchor 与方向，不是 cursor；预算不足时提高 `max-records` 至 `blockedEvent.requiredMaxRecords` 后重查。entry 保留索引中的完整 relations，因而 target 可以在本切片外；可选 relation `summary` 有值才出现，trace 不推断或补写它。判断当前适用性时，回到当前事实 owner，并按需综合相关报告。

## CLI 入口

从 skill 目录运行，或使用脚本绝对路径：

```text
node scripts/check-investigations.mjs [global-options] <command> [command-options]
node scripts/check-investigations.mjs help [command]
```

- 规范调用把全局选项放在 command 之前；在目标工作区内执行时省略 `--root`，默认以当前目录为工作区根，跨工作区调用才显式提供 `--root <workspace-root>`。
- `--investigations-dir` 只接受解析后仍在工作区内的相对路径；把集合目录误传给 `--root` 时，诊断会给出 `--root <workspace> --investigations-dir <relative-collection>` 的恢复形态。
- `help [command]` 与 `<command> --help` 只渲染帮助，不读取集合状态；省略 command 时渲染顶层帮助并以参数错误结束。
- 操作前用 `help <command>` 取得精确参数。本文负责报告形成与审阅，格式、命令前置和恢复范围沿“读取路径”取得。
