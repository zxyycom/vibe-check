### Case AUX-QUALITY-PARSER-001: parses scc 3.7 Provider paths and rejects unknown CSV headers
Entry:
- `src/product/quality-core/src/measurement/scanners.test.ts > quality scanner output parsing > parses scc 3.7 Provider paths and rejects unknown CSV headers`
Contract:
- Quality scanner parser fixtures 稳定 必须保持该原生测试节点界定的可观察行为：parses scc 3.7 Provider paths and rejects unknown CSV headers。
Proves:
- 在 `quality scanner output parsing` 下，该节点证明：parses scc 3.7 Provider paths and rejects unknown CSV headers。
