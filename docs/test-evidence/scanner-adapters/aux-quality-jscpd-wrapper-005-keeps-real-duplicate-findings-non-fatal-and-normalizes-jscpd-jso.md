### Case AUX-QUALITY-JSCPD-WRAPPER-005: keeps real duplicate findings non-fatal and normalizes jscpd JSON
Entry:
- `src/product/quality-core/src/measurement/scanners.test.ts > quality jscpd wrapper failure projection > keeps real duplicate findings non-fatal and normalizes jscpd JSON`
Contract:
- Quality jscpd wrapper failure projection 稳定 必须保持该原生测试节点界定的可观察行为：keeps real duplicate findings non-fatal and normalizes jscpd JSON。
Proves:
- 在 `quality jscpd wrapper failure projection` 下，该节点证明：keeps real duplicate findings non-fatal and normalizes jscpd JSON。
