### Case AUX-QUALITY-JSCPD-WRAPPER-003: classifies commands missing after preflight as execution failures
Entry:
- `src/product/quality-core/src/measurement/scanners.test.ts > quality jscpd wrapper failure projection > classifies commands missing after preflight as execution failures`
Contract:
- Quality jscpd wrapper failure projection 稳定 必须保持该原生测试节点界定的可观察行为：classifies commands missing after preflight as execution failures。
Proves:
- 在 `quality jscpd wrapper failure projection` 下，该节点证明：classifies commands missing after preflight as execution failures。
