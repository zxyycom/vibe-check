### Case AUX-QUALITY-JSCPD-TASK-001: plans one scan task per code area
Entry:
- `src/product/quality-core/src/measurement/scanners/jscpd/area-scans.test.ts > jscpd tasks > plans one scan task per code area`
Contract:
- Quality jscpd task planning 稳定 必须保持该原生测试节点界定的可观察行为：plans one scan task per code area。
Proves:
- 在 `jscpd tasks` 下，该节点证明：plans one scan task per code area。
