### Case AUX-QUALITY-JSCPD-WRAPPER-001: does not treat a successful jscpd run without JSON as a successful empty scan
Entry:
- `src/product/quality-core/src/measurement/scanners.test.ts > quality jscpd wrapper failure projection > does not treat a successful jscpd run without JSON as a successful empty scan`
Contract:
- Quality jscpd wrapper failure projection 稳定 必须保持该原生测试节点界定的可观察行为：does not treat a successful jscpd run without JSON as a successful empty scan。
Proves:
- 在 `quality jscpd wrapper failure projection` 下，该节点证明：does not treat a successful jscpd run without JSON as a successful empty scan。
