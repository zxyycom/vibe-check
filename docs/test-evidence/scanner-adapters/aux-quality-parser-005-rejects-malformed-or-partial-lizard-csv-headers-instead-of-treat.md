### Case AUX-QUALITY-PARSER-005: rejects malformed or partial Lizard CSV headers instead of treating them as zero functions
Entry:
- `src/product/quality-core/src/measurement/scanners.test.ts > quality scanner output parsing > rejects malformed or partial Lizard CSV headers instead of treating them as zero functions`
Contract:
- Quality scanner parser fixtures 稳定 必须保持该原生测试节点界定的可观察行为：rejects malformed or partial Lizard CSV headers instead of treating them as zero functions。
Proves:
- 在 `quality scanner output parsing` 下，该节点证明：rejects malformed or partial Lizard CSV headers instead of treating them as zero functions。
