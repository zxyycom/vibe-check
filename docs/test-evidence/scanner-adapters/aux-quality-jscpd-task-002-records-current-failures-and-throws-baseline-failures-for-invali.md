### Case AUX-QUALITY-JSCPD-TASK-002: records current failures and throws baseline failures for invalid jscpd output
Entry:
- `src/product/quality-core/src/measurement/scanners/jscpd/area-scans.test.ts > jscpd tasks > records current failures and throws baseline failures for invalid jscpd output`
Contract:
- Quality jscpd task planning 稳定 必须保持该原生测试节点界定的可观察行为：records current failures and throws baseline failures for invalid jscpd output。
Proves:
- 在 `jscpd tasks` 下，该节点证明：records current failures and throws baseline failures for invalid jscpd output。
