### Case WB-METRICS-GATE-EVALUATOR-001: applies the fixed disabled, completeness, and comparison priority
Entry:
- `src/product/quality-core/src/model/gate-evaluator.test.ts > gate evaluator prerequisites > applies the fixed disabled, completeness, and comparison priority`
Contract:
- Product gate evaluation 稳定 必须保持该原生测试节点界定的可观察行为：applies the fixed disabled, completeness, and comparison priority。
Proves:
- 在 `gate evaluator prerequisites` 下，该节点证明：applies the fixed disabled, completeness, and comparison priority。
