### Case BB-CLI-GATE-USAGE-001: returns exit 3 before scanners or artifacts for every invalid gate form
Entry:
- `src/product/cli.test.ts > gate CLI usage contract > returns exit 3 before scanners or artifacts for every invalid gate form`
Contract:
- Product gate usage failure 在启动前失败 必须保持该原生测试节点界定的可观察行为：returns exit 3 before scanners or artifacts for every invalid gate form。
Proves:
- 在 `gate CLI usage contract` 下，该节点证明：returns exit 3 before scanners or artifacts for every invalid gate form。
