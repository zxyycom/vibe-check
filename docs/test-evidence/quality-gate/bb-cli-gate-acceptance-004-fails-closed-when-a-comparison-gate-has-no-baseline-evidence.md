### Case BB-CLI-GATE-ACCEPTANCE-004: fails closed when a comparison gate has no baseline evidence
Entry:
- `src/product/cli-gate-acceptance.test.ts > formal CLI quality gate acceptance > fails closed when a comparison gate has no baseline evidence`
Contract:
- Product gate 正式入口跨 surface 可观察 必须保持该原生测试节点界定的可观察行为：fails closed when a comparison gate has no baseline evidence。
Proves:
- 在 `formal CLI quality gate acceptance` 下，该节点证明：fails closed when a comparison gate has no baseline evidence。
