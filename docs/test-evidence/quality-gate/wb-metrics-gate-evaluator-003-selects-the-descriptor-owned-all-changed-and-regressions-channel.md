### Case WB-METRICS-GATE-EVALUATOR-003: selects the descriptor-owned all, changed, and regressions channels
Entry:
- `src/product/quality-core/src/model/gate-evaluator.test.ts > gate evaluator warning selection > selects the descriptor-owned all, changed, and regressions channels`
Contract:
- Product gate evaluation 稳定 必须保持该原生测试节点界定的可观察行为：selects the descriptor-owned all, changed, and regressions channels。
Proves:
- 在 `gate evaluator warning selection` 下，该节点证明：selects the descriptor-owned all, changed, and regressions channels。
