### Case BB-CLI-GATE-OMITTED-005: --verification-output changes only the warning preview
Entry:
- `src/product/cli-omitted-gate-baseline.test.ts > formal CLI omitted-gate compatibility baseline > --verification-output changes only the warning preview`
Contract:
- Product omitted-gate 兼容性基线稳定 必须保持该原生测试节点界定的可观察行为：--verification-output changes only the warning preview。
Proves:
- 在 `formal CLI omitted-gate compatibility baseline` 下，该节点证明：--verification-output changes only the warning preview。
