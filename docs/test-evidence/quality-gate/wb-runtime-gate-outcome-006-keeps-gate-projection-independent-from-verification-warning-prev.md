### Case WB-RUNTIME-GATE-OUTCOME-006: keeps gate projection independent from verification warning preview
Entry:
- `src/product/quality-core/src/engine.test.ts > quality scan process outcome > keeps gate projection independent from verification warning preview`
Contract:
- Product gate process outcome 与 output priority 稳定 必须保持该原生测试节点界定的可观察行为：keeps gate projection independent from verification warning preview。
Proves:
- 在 `quality scan process outcome` 下，该节点证明：keeps gate projection independent from verification warning preview。
