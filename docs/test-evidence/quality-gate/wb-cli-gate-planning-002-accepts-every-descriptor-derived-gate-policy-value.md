### Case WB-CLI-GATE-PLANNING-002: accepts every descriptor-derived gate policy value
Entry:
- `src/product/args.test.ts > quality gate argument parsing and scan planning > accepts every descriptor-derived gate policy value`
Contract:
- Product gate parser、help 与 scan plan 稳定 必须保持该原生测试节点界定的可观察行为：accepts every descriptor-derived gate policy value。
Proves:
- 在 `quality gate argument parsing and scan planning` 下，该节点证明：accepts every descriptor-derived gate policy value。
