### Case WB-METRICS-GATE-EVALUATOR-004: treats input-unchanged as valid evidence for comparison policies
Entry:
- `src/product/quality-core/src/model/gate-evaluator.test.ts > gate evaluator warning selection > treats input-unchanged as valid evidence for comparison policies`
Contract:
- Product gate evaluation 稳定 必须保持该原生测试节点界定的可观察行为：treats input-unchanged as valid evidence for comparison policies。
Proves:
- 在 `gate evaluator warning selection` 下，该节点证明：treats input-unchanged as valid evidence for comparison policies。
