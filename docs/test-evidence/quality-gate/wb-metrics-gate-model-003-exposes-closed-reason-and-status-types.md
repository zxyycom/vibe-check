### Case WB-METRICS-GATE-MODEL-003: exposes closed reason and status types
Entry:
- `src/product/quality-core/src/model/gate-policy.test.ts > GateResult model > exposes closed reason and status types`
Contract:
- Product gate descriptor 与 result validation 稳定 必须保持该原生测试节点界定的可观察行为：exposes closed reason and status types。
Proves:
- 在 `GateResult model` 下，该节点证明：exposes closed reason and status types。
