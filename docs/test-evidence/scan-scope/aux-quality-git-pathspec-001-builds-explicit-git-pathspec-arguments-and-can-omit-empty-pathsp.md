### Case AUX-QUALITY-GIT-PATHSPEC-001: builds explicit git pathspec arguments and can omit empty pathspecs
Entry:
- `src/product/quality-core/src/input/files.test.ts > quality input git pathspecs > builds explicit git pathspec arguments and can omit empty pathspecs`
Contract:
- Quality git pathspec 参数稳定 必须保持该原生测试节点界定的可观察行为：builds explicit git pathspec arguments and can omit empty pathspecs。
Proves:
- 在 `quality input git pathspecs` 下，该节点证明：builds explicit git pathspec arguments and can omit empty pathspecs。
