### Case WB-SCOPE-FILE-COLLECTION-003: does not add built-in exclusions to the selected fallback config
Entry:
- `src/product/quality-core/src/input/files.test.ts > quality input file collection > does not add built-in exclusions to the selected fallback config`
Contract:
- Product current/baseline collection fallback 稳定 必须保持该原生测试节点界定的可观察行为：does not add built-in exclusions to the selected fallback config。
Proves:
- 在 `quality input file collection` 下，该节点证明：does not add built-in exclusions to the selected fallback config。
