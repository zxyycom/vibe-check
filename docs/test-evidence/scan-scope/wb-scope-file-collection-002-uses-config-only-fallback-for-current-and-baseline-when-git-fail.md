### Case WB-SCOPE-FILE-COLLECTION-002: uses config-only fallback for current and baseline when Git fails
Entry:
- `src/product/quality-core/src/input/files.test.ts > quality input file collection > uses config-only fallback for current and baseline when Git fails`
Contract:
- Product current/baseline collection fallback 稳定 必须保持该原生测试节点界定的可观察行为：uses config-only fallback for current and baseline when Git fails。
Proves:
- 在 `quality input file collection` 下，该节点证明：uses config-only fallback for current and baseline when Git fails。
