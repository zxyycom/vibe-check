### Case WB-SCOPE-FILE-COLLECTION-001: treats successful empty Git results as authoritative for current and baseline
Entry:
- `src/product/quality-core/src/input/files.test.ts > quality input file collection > treats successful empty Git results as authoritative for current and baseline`
Contract:
- Product current/baseline collection fallback 稳定 必须保持该原生测试节点界定的可观察行为：treats successful empty Git results as authoritative for current and baseline。
Proves:
- 在 `quality input file collection` 下，该节点证明：treats successful empty Git results as authoritative for current and baseline。
