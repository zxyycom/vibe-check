### Case AUX-QUALITY-FINGERPRINT-001: uses stable SHA-256 fingerprints for sorted file content
Entry:
- `src/product/quality-core/src/input/files.test.ts > quality input fingerprints > uses stable SHA-256 fingerprints for sorted file content`
Contract:
- Quality input fingerprint 稳定 必须保持该原生测试节点界定的可观察行为：uses stable SHA-256 fingerprints for sorted file content。
Proves:
- 在 `quality input fingerprints` 下，该节点证明：uses stable SHA-256 fingerprints for sorted file content。
