### Case AUX-QUALITY-DOGFOOD-001: keeps the dogfood wrapper pointed only at the product CLI
Entry:
- `src/product/cli.test.ts > formal and dogfood entrypoints > keeps the dogfood wrapper pointed only at the product CLI`
Contract:
- Quality dogfood package entries 保持 thin wrapper 必须保持该原生测试节点界定的可观察行为：keeps the dogfood wrapper pointed only at the product CLI。
Proves:
- 在 `formal and dogfood entrypoints` 下，该节点证明：keeps the dogfood wrapper pointed only at the product CLI。
