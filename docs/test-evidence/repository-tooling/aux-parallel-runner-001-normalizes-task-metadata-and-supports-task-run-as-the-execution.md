### Case AUX-PARALLEL-RUNNER-001: normalizes task metadata and supports task.run as the execution body
Entry:
- `scripts/tools/parallel-task-runner/test/index.test.ts > parallel task runner > normalizes task metadata and supports task.run as the execution body`
Contract:
- Parallel task runner 保持调度契约 必须保持该原生测试节点界定的可观察行为：normalizes task metadata and supports task.run as the execution body。
Proves:
- 在 `parallel task runner` 下，该节点证明：normalizes task metadata and supports task.run as the execution body。
