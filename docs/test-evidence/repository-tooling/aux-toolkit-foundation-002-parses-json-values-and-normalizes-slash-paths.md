### Case AUX-TOOLKIT-FOUNDATION-002: parses JSON values and normalizes slash paths
Entry:
- `scripts/tools/foundation/test/foundation.test.ts > script foundation > parses JSON values and normalizes slash paths`
Contract:
- 共享 script foundation helper 必须保持该原生测试节点界定的可观察行为：parses JSON values and normalizes slash paths。
Proves:
- 在 `script foundation` 下，该节点证明：parses JSON values and normalizes slash paths。
