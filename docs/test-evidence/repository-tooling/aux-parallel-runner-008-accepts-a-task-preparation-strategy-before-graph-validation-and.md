### Case AUX-PARALLEL-RUNNER-008: accepts a task preparation strategy before graph validation and scheduling
Entry:
- `scripts/tools/parallel-task-runner/test/index.test.ts > parallel task runner > accepts a task preparation strategy before graph validation and scheduling`
Contract:
- Parallel task runner 保持调度契约 必须保持该原生测试节点界定的可观察行为：accepts a task preparation strategy before graph validation and scheduling。
Proves:
- 在 `parallel task runner` 下，该节点证明：accepts a task preparation strategy before graph validation and scheduling。
