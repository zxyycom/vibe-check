### Case AUX-PARALLEL-RUNNER-009: rejects duplicate ids and unknown dependencies
Entry:
- `scripts/tools/parallel-task-runner/test/index.test.ts > parallel task runner > rejects duplicate ids and unknown dependencies`
Contract:
- Parallel task runner 保持调度契约 必须保持该原生测试节点界定的可观察行为：rejects duplicate ids and unknown dependencies。
Proves:
- 在 `parallel task runner` 下，该节点证明：rejects duplicate ids and unknown dependencies。
