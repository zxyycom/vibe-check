### Case AUX-PARALLEL-RUNNER-007: expands nested task groups with inherited metadata and group dependencies
Entry:
- `scripts/tools/parallel-task-runner/test/index.test.ts > parallel task runner > expands nested task groups with inherited metadata and group dependencies`
Contract:
- Parallel task runner 保持调度契约 必须保持该原生测试节点界定的可观察行为：expands nested task groups with inherited metadata and group dependencies。
Proves:
- 在 `parallel task runner` 下，该节点证明：expands nested task groups with inherited metadata and group dependencies。
