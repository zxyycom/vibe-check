### Case AUX-PARALLEL-RUNNER-010: rejects invalid task list metadata at the normalization boundary
Entry:
- `scripts/tools/parallel-task-runner/test/index.test.ts > parallel task runner > rejects invalid task list metadata at the normalization boundary`
Contract:
- Parallel task runner 保持调度契约 必须保持该原生测试节点界定的可观察行为：rejects invalid task list metadata at the normalization boundary。
Proves:
- 在 `parallel task runner` 下，该节点证明：rejects invalid task list metadata at the normalization boundary。
