### Case WB-CONFIG-FILE-004: loads a complete UTF-8 JSON file
Entry:
- `src/product/config-file.test.ts > quality config file loading > loads a complete UTF-8 JSON file`
Contract:
- Product 完整 JSON 配置 parsing 稳定 必须保持该原生测试节点界定的可观察行为：loads a complete UTF-8 JSON file。
Proves:
- 在 `quality config file loading` 下，该节点证明：loads a complete UTF-8 JSON file。
