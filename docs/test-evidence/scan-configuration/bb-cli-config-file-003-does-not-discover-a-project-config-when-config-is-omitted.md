### Case BB-CLI-CONFIG-FILE-003: does not discover a project config when --config is omitted
Entry:
- `src/product/configured-project.test.ts > formal CLI explicit configuration > does not discover a project config when --config is omitted`
Contract:
- Product 显式完整配置正式入口稳定 必须保持该原生测试节点界定的可观察行为：does not discover a project config when --config is omitted。
Proves:
- 在 `formal CLI explicit configuration` 下，该节点证明：does not discover a project config when --config is omitted。
