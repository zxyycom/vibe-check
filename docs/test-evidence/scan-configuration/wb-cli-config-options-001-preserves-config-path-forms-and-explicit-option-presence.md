### Case WB-CLI-CONFIG-OPTIONS-001: preserves config path forms and explicit option presence
Entry:
- `src/product/args.test.ts > product config argument parsing > preserves config path forms and explicit option presence`
Contract:
- Product config option presence 稳定 必须保持该原生测试节点界定的可观察行为：preserves config path forms and explicit option presence。
Proves:
- 在 `product config argument parsing` 下，该节点证明：preserves config path forms and explicit option presence。
