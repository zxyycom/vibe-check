### Case WB-CLI-CONFIG-OPTIONS-002: keeps config-dependent options absent when callers omit them
Entry:
- `src/product/args.test.ts > product config argument parsing > keeps config-dependent options absent when callers omit them`
Contract:
- Product config option presence 稳定 必须保持该原生测试节点界定的可观察行为：keeps config-dependent options absent when callers omit them。
Proves:
- 在 `product config argument parsing` 下，该节点证明：keeps config-dependent options absent when callers omit them。
