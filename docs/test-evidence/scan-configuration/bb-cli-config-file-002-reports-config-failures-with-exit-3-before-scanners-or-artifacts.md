### Case BB-CLI-CONFIG-FILE-002: reports config failures with exit 3 before scanners or artifacts start
Entry:
- `src/product/configured-project.test.ts > formal CLI explicit configuration > reports config failures with exit 3 before scanners or artifacts start`
Contract:
- Product 显式完整配置正式入口稳定 必须保持该原生测试节点界定的可观察行为：reports config failures with exit 3 before scanners or artifacts start。
Proves:
- 在 `formal CLI explicit configuration` 下，该节点证明：reports config failures with exit 3 before scanners or artifacts start。
