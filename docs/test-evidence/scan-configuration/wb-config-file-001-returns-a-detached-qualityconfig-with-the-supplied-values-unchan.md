### Case WB-CONFIG-FILE-001: returns a detached QualityConfig with the supplied values unchanged
Entry:
- `src/product/config-file.test.ts > complete quality config parsing > returns a detached QualityConfig with the supplied values unchanged`
Contract:
- Product 完整 JSON 配置 parsing 稳定 必须保持该原生测试节点界定的可观察行为：returns a detached QualityConfig with the supplied values unchanged。
Proves:
- 在 `complete quality config parsing` 下，该节点证明：returns a detached QualityConfig with the supplied values unchanged。
