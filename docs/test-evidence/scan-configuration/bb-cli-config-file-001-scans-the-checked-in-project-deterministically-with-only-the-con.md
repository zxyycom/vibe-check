### Case BB-CLI-CONFIG-FILE-001: scans the checked-in project deterministically with only the configured inputs
Entry:
- `src/product/configured-project.test.ts > formal CLI explicit configuration > scans the checked-in project deterministically with only the configured inputs`
Contract:
- Product 显式完整配置正式入口稳定 必须保持该原生测试节点界定的可观察行为：scans the checked-in project deterministically with only the configured inputs。
Proves:
- 在 `formal CLI explicit configuration` 下，该节点证明：scans the checked-in project deterministically with only the configured inputs。
