### Case AUX-QUALITY-WARNINGS-001: uses scc code lines and low decision-token allowance for file-size warnings
Entry:
- `src/product/quality-core/src/output/warnings/generator.test.ts > quality warning generation > uses scc code lines and low decision-token allowance for file-size warnings`
Contract:
- Quality warning 阈值语义稳定 必须保持该原生测试节点界定的可观察行为：uses scc code lines and low decision-token allowance for file-size warnings。
Proves:
- 在 `quality warning generation` 下，该节点证明：uses scc code lines and low decision-token allowance for file-size warnings。
