### Case AUX-QUALITY-WARNINGS-003: adds configured accepted reasons without relying on duplicate line numbers
Entry:
- `src/product/quality-core/src/output/warnings/generator.test.ts > quality warning generation > adds configured accepted reasons without relying on duplicate line numbers`
Contract:
- Quality warning 阈值语义稳定 必须保持该原生测试节点界定的可观察行为：adds configured accepted reasons without relying on duplicate line numbers。
Proves:
- 在 `quality warning generation` 下，该节点证明：adds configured accepted reasons without relying on duplicate line numbers。
