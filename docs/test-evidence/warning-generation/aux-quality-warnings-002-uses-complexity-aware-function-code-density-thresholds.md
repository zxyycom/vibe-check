### Case AUX-QUALITY-WARNINGS-002: uses complexity-aware function code density thresholds
Entry:
- `src/product/quality-core/src/output/warnings/generator.test.ts > quality warning generation > uses complexity-aware function code density thresholds`
Contract:
- Quality warning 阈值语义稳定 必须保持该原生测试节点界定的可观察行为：uses complexity-aware function code density thresholds。
Proves:
- 在 `quality warning generation` 下，该节点证明：uses complexity-aware function code density thresholds。
