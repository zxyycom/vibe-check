### Case AUX-QUALITY-REPORT-009: keeps disabled gates silent in human reports
Entry:
- `src/product/quality-core/src/output/report/markdown-report.test.ts > quality report > keeps disabled gates silent in human reports`
Contract:
- Quality report 排名和 changed-file 摘要稳定 必须保持该原生测试节点界定的可观察行为：keeps disabled gates silent in human reports。
Proves:
- 在 `quality report` 下，该节点证明：keeps disabled gates silent in human reports。
