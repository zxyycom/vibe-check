### Case AUX-QUALITY-REPORT-003: sorts rankings by metric without mutating scanner output order
Entry:
- `src/product/quality-core/src/output/report/markdown-report.test.ts > quality report > sorts rankings by metric without mutating scanner output order`
Contract:
- Quality report 排名和 changed-file 摘要稳定 必须保持该原生测试节点界定的可观察行为：sorts rankings by metric without mutating scanner output order。
Proves:
- 在 `quality report` 下，该节点证明：sorts rankings by metric without mutating scanner output order。
