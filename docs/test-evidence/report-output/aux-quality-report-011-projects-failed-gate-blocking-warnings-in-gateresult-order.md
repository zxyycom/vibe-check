### Case AUX-QUALITY-REPORT-011: projects failed gate blocking warnings in GateResult order
Entry:
- `src/product/quality-core/src/output/report/markdown-report.test.ts > quality report > projects failed gate blocking warnings in GateResult order`
Contract:
- Quality report 排名和 changed-file 摘要稳定 必须保持该原生测试节点界定的可观察行为：projects failed gate blocking warnings in GateResult order。
Proves:
- 在 `quality report` 下，该节点证明：projects failed gate blocking warnings in GateResult order。
