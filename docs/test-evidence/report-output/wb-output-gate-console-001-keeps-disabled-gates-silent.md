### Case WB-OUTPUT-GATE-CONSOLE-001: keeps disabled gates silent
Entry:
- `src/product/quality-core/src/scan-command/command-output.test.ts > gate console projection > keeps disabled gates silent`
Contract:
- Product gate console projection 稳定 必须保持该原生测试节点界定的可观察行为：keeps disabled gates silent。
Proves:
- 在 `gate console projection` 下，该节点证明：keeps disabled gates silent。
