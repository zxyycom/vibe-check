### Case BB-CLI-CHANGED-FILES-001: maps wrapped read errors to ordinary and missing-input exits
Entry:
- `src/product/cli.test.ts > changed-files CLI contract > maps wrapped read errors to ordinary and missing-input exits`
Contract:
- Product changed-files CLI 路径与错误映射稳定 必须保持该原生测试节点界定的可观察行为：maps wrapped read errors to ordinary and missing-input exits。
Proves:
- 在 `changed-files CLI contract` 下，该节点证明：maps wrapped read errors to ordinary and missing-input exits。
