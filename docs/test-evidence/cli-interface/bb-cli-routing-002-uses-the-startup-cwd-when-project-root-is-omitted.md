### Case BB-CLI-ROUTING-002: uses the startup cwd when project root is omitted
Entry:
- `src/product/cli.test.ts > product CLI routing > uses the startup cwd when project root is omitted`
Contract:
- Product CLI routing 与顶层错误映射 必须保持该原生测试节点界定的可观察行为：uses the startup cwd when project root is omitted。
Proves:
- 在 `product CLI routing` 下，该节点证明：uses the startup cwd when project root is omitted。
