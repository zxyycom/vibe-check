### Case BB-CLI-CHANGED-FILES-003: resolves a relative changed-files list from an explicit project root
Entry:
- `src/product/cli.test.ts > changed-files CLI contract > resolves a relative changed-files list from an explicit project root`
Contract:
- Product changed-files CLI 路径与错误映射稳定 必须保持该原生测试节点界定的可观察行为：resolves a relative changed-files list from an explicit project root。
Proves:
- 在 `changed-files CLI contract` 下，该节点证明：resolves a relative changed-files list from an explicit project root。
