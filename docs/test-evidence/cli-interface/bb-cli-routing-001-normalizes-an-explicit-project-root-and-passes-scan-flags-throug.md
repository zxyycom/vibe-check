### Case BB-CLI-ROUTING-001: normalizes an explicit project root and passes scan flags through unchanged
Entry:
- `src/product/cli.test.ts > product CLI routing > normalizes an explicit project root and passes scan flags through unchanged`
Contract:
- Product CLI routing 与顶层错误映射 必须保持该原生测试节点界定的可观察行为：normalizes an explicit project root and passes scan flags through unchanged。
Proves:
- 在 `product CLI routing` 下，该节点证明：normalizes an explicit project root and passes scan flags through unchanged。
