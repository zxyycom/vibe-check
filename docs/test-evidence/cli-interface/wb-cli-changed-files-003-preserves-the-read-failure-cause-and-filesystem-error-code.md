### Case WB-CLI-CHANGED-FILES-003: preserves the read failure cause and filesystem error code
Entry:
- `src/product/quality-core/src/input/files.test.ts > quality changed file input > preserves the read failure cause and filesystem error code`
Contract:
- Product changed-file input 路径与错误边界稳定 必须保持该原生测试节点界定的可观察行为：preserves the read failure cause and filesystem error code。
Proves:
- 在 `quality changed file input` 下，该节点证明：preserves the read failure cause and filesystem error code。
