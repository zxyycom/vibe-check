### Case BB-CLI-ROUTING-004: writes top-level errors to stderr and preserves ordinary and special mappings
Entry:
- `src/product/cli.test.ts > product CLI routing > writes top-level errors to stderr and preserves ordinary and special mappings`
Contract:
- Product CLI routing 与顶层错误映射 必须保持该原生测试节点界定的可观察行为：writes top-level errors to stderr and preserves ordinary and special mappings。
Proves:
- 在 `product CLI routing` 下，该节点证明：writes top-level errors to stderr and preserves ordinary and special mappings。
