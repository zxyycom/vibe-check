### Case WB-CLI-CHANGED-FILES-002: allows absolute and parent-relative list paths while keeping project-relative entries
Entry:
- `src/product/quality-core/src/input/files.test.ts > quality changed file input > allows absolute and parent-relative list paths while keeping project-relative entries`
Contract:
- Product changed-file input 路径与错误边界稳定 必须保持该原生测试节点界定的可观察行为：allows absolute and parent-relative list paths while keeping project-relative entries。
Proves:
- 在 `quality changed file input` 下，该节点证明：allows absolute and parent-relative list paths while keeping project-relative entries。
