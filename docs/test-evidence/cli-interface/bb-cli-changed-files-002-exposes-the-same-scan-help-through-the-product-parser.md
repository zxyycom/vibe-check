### Case BB-CLI-CHANGED-FILES-002: exposes the same scan help through the product parser
Entry:
- `src/product/cli.test.ts > changed-files CLI contract > exposes the same scan help through the product parser`
Contract:
- Product changed-files CLI 路径与错误映射稳定 必须保持该原生测试节点界定的可观察行为：exposes the same scan help through the product parser。
Proves:
- 在 `changed-files CLI contract` 下，该节点证明：exposes the same scan help through the product parser。
