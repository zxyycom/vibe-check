### Case WB-RUNTIME-CAPABILITY-RESULT-007: keeps jscpd non-zero exits as execution failures when stderr mentions reports
Entry:
- `src/product/quality-core/src/measurement/current-revision/current-revision.test.ts > current revision scanner failure projection > keeps jscpd non-zero exits as execution failures when stderr mentions reports`
Contract:
- Product current capability result 投影稳定 必须保持该原生测试节点界定的可观察行为：keeps jscpd non-zero exits as execution failures when stderr mentions reports。
Proves:
- 在 `current revision scanner failure projection` 下，该节点证明：keeps jscpd non-zero exits as execution failures when stderr mentions reports。
