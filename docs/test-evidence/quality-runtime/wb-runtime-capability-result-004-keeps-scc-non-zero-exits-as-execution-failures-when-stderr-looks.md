### Case WB-RUNTIME-CAPABILITY-RESULT-004: keeps scc non-zero exits as execution failures when stderr looks like a parser error
Entry:
- `src/product/quality-core/src/measurement/current-revision/current-revision.test.ts > current revision scanner failure projection > keeps scc non-zero exits as execution failures when stderr looks like a parser error`
Contract:
- Product current capability result 投影稳定 必须保持该原生测试节点界定的可观察行为：keeps scc non-zero exits as execution failures when stderr looks like a parser error。
Proves:
- 在 `current revision scanner failure projection` 下，该节点证明：keeps scc non-zero exits as execution failures when stderr looks like a parser error。
