### Case BB-RUNTIME-COMPLETENESS-001: returns a warning without a quality verdict when no capability has eligible input
Entry:
- `src/product/configured-project.test.ts > formal CLI explicit configuration > returns a warning without a quality verdict when no capability has eligible input`
Contract:
- Product scan completeness 跨 surface 可观察 必须保持该原生测试节点界定的可观察行为：returns a warning without a quality verdict when no capability has eligible input。
Proves:
- 在 `formal CLI explicit configuration` 下，该节点证明：returns a warning without a quality verdict when no capability has eligible input。
