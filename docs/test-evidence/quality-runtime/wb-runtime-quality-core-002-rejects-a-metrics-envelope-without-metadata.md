### Case WB-RUNTIME-QUALITY-CORE-002: rejects a metrics envelope without metadata
Entry:
- `src/product/quality-core/test/quality-core.test.ts > script quality core > rejects a metrics envelope without metadata`
Contract:
- Quality core schema 与状态归约 必须保持该原生测试节点界定的可观察行为：rejects a metrics envelope without metadata。
Proves:
- 在 `script quality core` 下，该节点证明：rejects a metrics envelope without metadata。
