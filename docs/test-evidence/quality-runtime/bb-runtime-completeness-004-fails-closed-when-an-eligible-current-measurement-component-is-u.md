### Case BB-RUNTIME-COMPLETENESS-004: fails closed when an eligible current measurement component is unavailable
Entry:
- `src/product/configured-project.test.ts > formal CLI explicit configuration > fails closed when an eligible current measurement component is unavailable`
Contract:
- Product scan completeness 跨 surface 可观察 必须保持该原生测试节点界定的可观察行为：fails closed when an eligible current measurement component is unavailable。
Proves:
- 在 `formal CLI explicit configuration` 下，该节点证明：fails closed when an eligible current measurement component is unavailable。
