### Case WB-RUNTIME-QUALITY-CORE-001: classifies files using caller-provided code areas
Entry:
- `src/product/quality-core/test/quality-core.test.ts > script quality core > classifies files using caller-provided code areas`
Contract:
- Quality core schema 与状态归约 必须保持该原生测试节点界定的可观察行为：classifies files using caller-provided code areas。
Proves:
- 在 `script quality core` 下，该节点证明：classifies files using caller-provided code areas。
