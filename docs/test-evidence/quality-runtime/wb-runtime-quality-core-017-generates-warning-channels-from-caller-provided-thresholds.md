### Case WB-RUNTIME-QUALITY-CORE-017: generates warning channels from caller-provided thresholds
Entry:
- `src/product/quality-core/test/quality-core.test.ts > script quality core > generates warning channels from caller-provided thresholds`
Contract:
- Quality core schema 与状态归约 必须保持该原生测试节点界定的可观察行为：generates warning channels from caller-provided thresholds。
Proves:
- 在 `script quality core` 下，该节点证明：generates warning channels from caller-provided thresholds。
