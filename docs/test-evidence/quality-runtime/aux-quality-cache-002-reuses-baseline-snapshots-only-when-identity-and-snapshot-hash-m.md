### Case AUX-QUALITY-CACHE-002: reuses baseline snapshots only when identity and snapshot hash match
Entry:
- `src/product/quality-core/src/measurement/cache.test.ts > quality measurement cache > reuses baseline snapshots only when identity and snapshot hash match`
Contract:
- Quality measurement cache identity 稳定 必须保持该原生测试节点界定的可观察行为：reuses baseline snapshots only when identity and snapshot hash match。
Proves:
- 在 `quality measurement cache` 下，该节点证明：reuses baseline snapshots only when identity and snapshot hash match。
