### Case WB-RUNTIME-QUALITY-CORE-016: accepted warnings pass verification while the quality check remains a warning
Entry:
- `src/product/quality-core/test/quality-core.test.ts > script quality core > accepted warnings pass verification while the quality check remains a warning`
Contract:
- Quality core schema 与状态归约 必须保持该原生测试节点界定的可观察行为：accepted warnings pass verification while the quality check remains a warning。
Proves:
- 在 `script quality core` 下，该节点证明：accepted warnings pass verification while the quality check remains a warning。
