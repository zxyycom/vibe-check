### Case WB-RUNTIME-QUALITY-CORE-013: maps complete verification with warnings=true to warning
Entry:
- `src/product/quality-core/test/quality-core.test.ts > script quality core > maps complete verification with warnings=true to warning`
Contract:
- Quality core schema 与状态归约 必须保持该原生测试节点界定的可观察行为：maps complete verification with warnings=true to warning。
Proves:
- 在 `script quality core` 下，该节点证明：maps complete verification with warnings=true to warning。
