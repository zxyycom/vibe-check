### Case WB-RUNTIME-BASELINE-001: resolves an eligible baseline tool when the current revision has no input for it
Entry:
- `src/product/quality-core/src/measurement/baseline-revision.test.ts > baseline revision capability eligibility > resolves an eligible baseline tool when the current revision has no input for it`
Contract:
- Baseline capability eligibility 必须保持该原生测试节点界定的可观察行为：resolves an eligible baseline tool when the current revision has no input for it。
Proves:
- 在 `baseline revision capability eligibility` 下，该节点证明：resolves an eligible baseline tool when the current revision has no input for it。
