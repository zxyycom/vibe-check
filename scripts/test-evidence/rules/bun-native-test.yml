id: bun-native-test
message: Supported Bun test entity
severity: info
language: TypeScript
rule:
  any:
    - pattern: test("$NAME", $RUN)
    - pattern: it("$NAME", $RUN)
    - pattern: test("$NAME", $OPTIONS, $RUN)
    - pattern: it("$NAME", $OPTIONS, $RUN)
