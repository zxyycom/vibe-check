pub fn assemble(
    alpha: i32,
    beta: i32,
    gamma: i32,
    delta: i32,
    epsilon: i32,
) -> i32 {
    alpha + beta + gamma + delta + epsilon
}

pub fn below_threshold(alpha: i32, beta: i32, gamma: i32, delta: i32) -> i32 {
    alpha + beta + gamma + delta
}
