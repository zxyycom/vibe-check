# Tasks

任务在 strict JSON boundary后完成 registry/binding、离线 2020-12 engine、safe Records 与首版 package证据。

## Readiness

- [x] 0.1 已按当前 ordinary Check/options/Record/scope contract重建范围，并移除旧 shared policy、TaskPlan、comparison/cache与 Record catalog前置。
- [x] 0.2 已固定显式 registry/binding、registered-only zero-network、safe authoring IDs、四类 issue与首版非目标。
- [ ] 0.3 在 JSON Change helper可用后，运行 Test Evidence起点检查并审计 Ajv 2020-12 entry、Bun/installed runtime、license、reference行为和credential redaction。

## Implementation

- [ ] 1.1 先新增 options/scope/document/compile/ref/keyword/zero-network fixtures，再实现 immutable registry/binding normalization。
- [ ] 1.2 复用 strict JSON helper，封装同步 2020-12 engine adapter、registered-only resolver与 safe normalized issues。
- [ ] 1.3 新增 `jsonSchemaValidation` value、`JsonSchemaValidationOptions`、runtime validation、Records、final counts和四态结果。
- [ ] 1.4 同步 public exports/contract、owner docs、README/JSDoc/examples、production dependency/license、semantic Cases与 isolated consumer。

## Verification

- [ ] 2.1 运行 document/compile/ref/keyword/options/scope/issue-cap/zero-network/credential-canary最窄 tests与 Test Evidence closure。
- [ ] 2.2 运行 product typecheck、lint、tests、docs/package candidate与 installed Bun acceptance。
- [ ] 2.3 运行 required/full Gate，复核 artifacts/logs/errors不含 raw schema/instance bytes、URI credentials、absolute paths或 engine-native messages。
