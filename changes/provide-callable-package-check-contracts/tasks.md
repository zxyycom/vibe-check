# Tasks

任务先关闭公共与消息契约，再实施各 Check，最后同步消费面和完整验证。

## Readiness

- [x] 0.1 恢复 Configuration、Quality Metrics、scanner、coding、testing、Decision 与 package documentation owners，并完成起点 Test Evidence closure。
- [x] 0.2 确认共享层只统一函数、parser 和 message 义务，各 Check 的 authoring/resolved options 与结果类型保持 owner-local。

## Implementation

- [x] 1.1 将 JSON、JSON Schema 与 Markdown Link 改为补默认值的函数，并硬切全部调用点。
- [x] 1.2 为七项随包 Check 增加严格 final-data parsers、返回 Check `parseData` 与必要公共辅助类型。
- [x] 1.3 为七项随包 Check 的 Check-owned 问题增加安全可行动 messages，并验证敏感边界。
- [x] 1.4 同步 public inventory、JSDoc、README/API/Check guides、package examples 和 SCC/Lizard 安装说明。
- [x] 1.5 更新直接行为测试与 Semantic Case proof，使 constructor、parser、message 和 external consumer 证据闭合。

## Verification

- [x] 2.1 运行最窄 package Check、public inventory、documentation 与 external consumer tests。
- [x] 2.2 运行 Test Evidence、docs validation、typecheck、lint、Decision 和 Change checks。
- [x] 2.3 重建 exact candidate 并通过 full package verification，确认安装后 declarations、docs 与 runtime 同步。
