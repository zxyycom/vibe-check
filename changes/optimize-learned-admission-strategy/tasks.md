# Tasks

任务按「先冻结可比较证据，再隔离实现，先由 deterministic gates 决定 adopt path 是否开放，最后才执行 Gate A/B 或 not-adopt 收口」排序；所有 checkbox 当前均未完成。

## Readiness

- [ ] 0.1 复核 `task-scheduler` 现行 strict baseline：记录 first nonempty layer 为 tightening→continuation→ordinary；constrained exact comparator 为 scope cap↑→score↓→priority↓→Task ID↑→scope ID↑→Task ID↑（最后一项为现实现 duplicate Task-ID fallback，unique Task ID 时不可达，oracle 不得把 scope ID 提前）；ordinary 为 score↓→priority↓→Task ID↑；并固定 first-only `canAdmit=false → wait` 的 direct-policy trace。Owner: `src/project-run/task-scheduler/**`。
- [ ] 0.2 复核当前 private prediction/admission seam：以 runtime、`docs/development/architecture.md`、`docs/api-mechanics.md` 与 active+aligned lifecycle Decision 确认所选稳定提交包含该 seam；归档 Change 只作形成时证据，不能替代当前 owner。该提交进入本 Change 实施基线后才解除 production wiring Readiness；在此之前只允许 corpus/harness evidence。确认 simulation public API 非依赖，fail-fast/named-capacity 仍 Draft；若任一后两者先落地，停止并 rebaseline。Owner: Change coordination + duration-model/provider owner。
- [ ] 0.3 冻结 deterministic corpus format：每个 fixture 保存 immutable graph、initial inspection/state、frozen prediction snapshot digest、prediction-derived critical-path score table及其来源/provenance、settlement/timing script、expected baseline/candidate decision trace、expected admission/settlement timing trace、membership、terminal outcome、fingerprint与summary。Owner: private task-scheduler comparison harness。
- [ ] 0.4 建立 no-record comparison path；若不能做到，则建立 per-variant isolated state，并证明同一 frozen prediction/score table 未被 Run 记录改写。每次 trace 必须带 prediction/score provenance/digest；两个 custom callback只读同一 score table，且不得使用或更新 mutable live history。Owner: duration-model/provider owner。
- [ ] 0.5 预注册 deterministic corpus 的 exact assertions（每项均由 private task-scheduler harness owner 负责）：(a) ordinary score/priority/ID：选择/排序 trace 精确等于 score↓→priority↓→ID；timing trace 在同一 virtual script 下相同；(b) tightening：只从 tightening layer选择，scope-cap↑→score↓→priority↓→Task ID↑→scope ID↑→Task ID↑（final duplicate Task-ID fallback 在 unique ID 下不可达）trace精确；timing断言为该 layer 的 first boundary；(c) continuation：只从 active constrained continuation选择，scope-cap↑→score↓→priority↓→Task ID↑→scope ID↑→Task ID↑（final duplicate Task-ID fallback 在 unique ID 下不可达）排序/activation trace精确；timing断言为 scope terminal/continuation boundary；(d) `dependsOn`/`observes`：未 relation-eligible Task 不出现在 candidate/selection trace；dependent/observer 只在合法 settlement 后出现，admission timestamp 精确；(e) mutex/root capacity：无效 select 被 hard guard拒绝、strict wait 可 drain、candidate select 必须 `canAdmit=true`，并断言 root/mutex slot interval；(f) scope unsafe-backfill witness：保存 strict wait 与同层可 admission backfill trace，若 prescribed settlement 使 protected preferred Task 较 strict 更晚 admission，即记录为 protected-delay regression 和 not-adopt evidence，绝不声称 safety guarantee；(g) benefit/risk pair：相同 graph/prediction 下保存一个 utilization/elapsed benefit trace和一个 protected-delay/tail risk trace，分别断言 exact delta；(h) failure/blocked/cancel/finite progress：断言 failure、blocked、cancel terminal outcome 不变，wait 仅在 running 可 drain，且每个合法 select 缩小 pending 集合直至 finite terminal；(i) frozen prediction：两 variants read identical digest，不能产生 prediction/history write。
- [ ] 0.6 对 0.5 所有 fixtures 逐项指定 protected Task、designated benefit metric、virtual-time extraction和 expected timing unit；冻结必须匹配的 trace/membership/outcome/fingerprint/summary fields及不匹配即 invalid 的规则。Owner: task-scheduler evidence owner。
- [ ] 0.7 预注册 same-host Gate A/B extraction：正式 `bun run check` 与 `bun run check -- --all` 的 outer wall 是 primary endpoint；`elapsed-to-initial-result` 与 Scheduler utilization/tail 等仅作归因。designated benefits包含至少一个 workload outer-wall改善或 preregistered scheduler attribution改善；protected cases及其 admission-delay extraction在采样前列名。不得把 advisory threshold 当 budget。若定义 pure decision/control numeric guard，基于 strict baseline 的 per-decision/control measurements、candidate count/input size、repeat count和统计量在采样前写入推导与阈值；否则仍须以同 host traces解释无复杂度/明显回归。Owner: Gate/performance evidence owner。
- [ ] 0.8 冻结 A/B data contract与排除规则：同一 exact reused installed package candidate/receipt、same membership/outcome、frozen prediction/no-record或isolated state、timing available；两个 custom variant的完整 experimental custom Definition fingerprint必须相同，且必须不同于 production learned fingerprint。每 variant/selection warm-up 一次并排除。唯一五组序列为奇数组 baseline-required→candidate-required→baseline-all→candidate-all，偶数组 candidate-required→baseline-required→candidate-all→baseline-all。每 sample 保存 script base/variant bytes/hash、base/variant tracked-diff fingerprint、完整 custom Definition fingerprint与raw log；只允许明确的环境/command failure排除，保留其 raw log及原因，不能用不利 timing/trace删样。Owner: Gate/performance evidence owner。

## Implementation

- [ ] 1.1 在 private comparison harness 实现 strict baseline oracle，逐字保留 first-nonempty layer、existing comparator 与 first-only wait 行为；不接入 production。Owner: `src/project-run/task-scheduler/**`。
- [ ] 1.2 在同一 harness 实现唯一 `same-layer admissible-first backfill`：首选可 admission照旧 select；否则仅在同 layer ordered candidates取第一个 `canAdmit`；无则 wait。禁止跨层、state/reservation/clock/public field/registry。Owner: `src/project-run/task-scheduler/**`。
- [ ] 1.3 为 0.5 corpus 实现 fixture loader、frozen prediction injection、virtual settlement/timing driver与 trace recorder；记录 decision proposal、Scheduler hard guard、admission/settlement timestamps、membership、terminal outcome、fingerprint和summary。Owner: private comparison harness。
- [ ] 1.4 在 `scripts/project/gate/**` 实现 comparison-only custom adapter/score fixture：central Gate Definition的 strict/candidate variants均为现有 `{ kind: "custom", proposeAdmission }`，共用0.3的冻结 score table；strict callback严格回放current first-layer/comparator/first-only wait，candidate回放same-layer admissible-first。使用既有public custom context的graph、`candidates.canAdmit`、capacity、activeScope和running facts；逐 trace 对照private strict/candidate harness，证明只是evidence projection、不重写Scheduler legality或成为Product algorithm owner；它不是Product API。Owner: script/evidence + task-scheduler owner。
- [ ] 1.5 实现 Change-local/evidence-only script variant runner，只能在目标 Change独占worktree运行：启动时记录 `scripts/project/gate/definition.ts`（及必要的一条direct assertion）的base bytes/hash和预期variant bytes/hash；只切换这些script bytes，不改`src/**`或package inputs。restore前current bytes不是预期variant则fail并保留现场；仅匹配时在`finally`原子恢复base bytes并验证hash。禁止`git reset`/`git restore`、public config/env switch、registry或hidden env/runtime selector。Owner: Gate/script evidence owner。
- [ ] 1.6 运行 deterministic corpus和adapter oracle对照，保存baseline/candidate raw traces与每项 assertion结果；任何 correctness/hard guard/terminal/finite-progress failure，或scope unsafe-backfill witness的protected-delay regression，都立即停止 adopt path、记录not-adopt，并禁止后续 Gate candidate sampling。Owner: task-scheduler/script evidence owner。

## Verification

- [ ] 2.1 运行最窄 direct policy/harness/adapter/runner tests，逐项核对 0.5 的 exact decision/timing/terminal assertions、1.4 adapter-to-harness oracle和1.5 safe-restore assertion；确认 adapter/candidate 没有 bypass `canAdmit`、hard guard、mutex、root capacity、cancellation或settlement。若 scope unsafe-backfill witness 的 protected delay退化，确认 1.6 已关闭 adopt path且不进入 2.2。Owner: task-scheduler/script test owner。
- [ ] 2.2 仅当 2.1/1.6 证明 deterministic adopt path 仍开放时，通过1.5 script runner执行 required/all A/B：每 variant/workload先 warm-up 一次并排除；奇数组（1、3、5）严格为 baseline-required→candidate-required→baseline-all→candidate-all，偶数组（2、4）严格为 candidate-required→baseline-required→candidate-all→baseline-all。用正式 `bun run check` 与 `bun run check -- --all` 保存每一 run 的原始 log、ordered trace、membership、outcome、prediction/score provenance、script base/variant bytes/hash、base/variant tracked-diff fingerprint、完整 experimental custom Definition fingerprint、exact reused installed candidate/receipt、scheduler summary、formal outer wall（primary）和 `elapsed-to-initial-result`（attribution）；每次 `finally` 后仅在expected variant bytes仍匹配时原子恢复base并确认hash，否则fail并保留现场。两个variant同为custom、承受同一per-decision measurement overhead；A/B主要测installed package central Gate Scheduler，reused candidate内nested consumer在两variant相同。Owner: Gate/performance evidence owner。
- [ ] 2.3 在每 selection 计算 formal outer-wall paired statistics：candidate paired median≤baseline、至少一 selection改善≥5%、至少4/5 pairs candidate不差、nearest-rank p95≤baseline 105%；同时检查所有 designated benefit至少一项改善，及全部 preregistered protected admission delay不退化。将 initial/Scheduler metrics仅作为归因输出，绝不将 advisory threshold视为 budget。Owner: Gate/performance evidence owner。
- [ ] 2.4 审计 pure decision/control cost：在同 host、同 frozen input和candidate-count下比较 strict/candidate；说明 measurement、复杂度与任何差异。未解释的复杂度或明显回归使候选 not-adopt。Owner: task-scheduler performance owner。
- [ ] 2.5 仅在2.1–2.4的 gates、当前 private seam 已进入实施基线与另行 implementation authorization均通过后，才在 `src/project-run/task-scheduler/**` 将candidate作为最小private production binding；其实现必须再次对1.4同一oracle。随后重新 build package candidate，并运行受影响 direct tests和 learned production all installed-consumer acceptance。script adapter/runner 不属于此 wiring；Gate advisory threshold不作为 budget。Owner: integration/Gate owner。
- [ ] 2.6 形成采用结论：adopt 时记录满足的每项 gate、candidate fingerprint和后续 Decision review需求；not-adopt 时记录失败的 gate、保留 strict baseline、确认未改 production runtime/public API/Decision，移除 adapter/runner/candidate code/candidate-only wiring/无独立价值 abstraction并保留可复核 tests/evidence。任何保留的 neutral harness必须说明其独立证明价值和无额外抽象。Owner: Change owner。
