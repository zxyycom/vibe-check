# Tasks

按三道 contract lock 与四个检查点推进，最后完成 public hard cut 和整体验收。只有任务产物与所列最窄证据同时存在时才勾选；前一检查点未放行时，不扩大到后一检查点。

## Readiness

- [x] 0.1 已核对 proposal、design 与 tasks 指向“独立 Check / Record managers，policy 只消费 final Core snapshot，所有 public surfaces 只投影一个 validated publication model”这一目标；范围不包含 Project Definition、TaskPlan 或 feature algorithms。
- [x] 0.2 已读取当前 `docs/` owners、`src/product/**` 实现和直接相关活动决策，并区分 current capability / machine-v1事实与本 Change 的目标 delta；历史材料不作为当前实现输入。
- [x] 0.3 已复核 Scan Scope / Scanner Dependency handoff：Product-owned config glob与 current / baseline exact inputs、source-scoped result acceptance、payload-private location、cache与 backend identity必须在 built-in迁移中保持。
- [x] 0.4 已复核直接下游 `establish-check-task-orchestration` 与 `adopt-typescript-project-definition` 的依赖 seam；本 Change只提供 frozen catalog / binding、manager ports、terminal reports、finalization、closed policy与 public / private identity边界，不提前实现 scheduler或 authoring loader。
- [x] 0.5 已确认 current `all | changed | regressions` intent、显式 baseline prerequisite、existing flags / profiles、`--verification-output` preview-only行为、human quality / verification status、GateResult / process-exit mapping与 `raw/**`定位保持；machine v2、legacy artifact cleanup、warning / capability移除是 intentional hard cut，不提供 dual path。
- [x] 0.6 计划审计门禁已闭合：foundation、policy / reference、publication的 exact grammar分别由依赖代码之前的 contract lock承接；四个内部检查点均有进入条件、产物、放行证据与设计回写条件；没有阻塞范围、owner、兼容方向或验收出口的开放问题。

## Implementation

- [x] 1.1 在修改测试正文 / test nodes / Cases前运行 `bun run test-evidence:check`，按 `test-evidence-review` 恢复相关 Cases；实施开始后，在 `design.md` 追加的 `Implementation Observations` 中持续维护迁移矩阵，逐项映射 current capability result、completeness、warning channels、gate modes、machine-v1 artifacts、report / console、CLI / annotation / dogfood consumers到目标 Check / Record / policy / machine-v2 owner及其删除证据。
- [x] 1.2 完成 foundation contract lock：以 model / validator目标测试固定 `CheckDefinition`、record-type descriptor、`CheckRun` / `CheckResult`、`QualityRecord`、reference-free final Core snapshot及其 integrity / completeness状态的 normalized grammar，以及 check / record-type / run / record identities、canonical identity bytes、catalog fingerprint、diagnostic categories与 same-run failure precedence；先确认 contract tests能拒绝错误实现并完成 contract diff审阅，再实现 managers。
- [x] 1.3 在 `src/product/**` 实现 serializable Check / record-type catalog、canonical validation / fingerprint与独立 private binding registry；在 work前拒绝非法 identity / descriptor、unknown / duplicate catalog成员和 missing / extra binding，并冻结完整 definitions、bindings、selection与 applicability。
- [x] 1.4 实现 CheckManager的每-definition单一 invocation run、pre-work applicability-owned `not-applicable`、opaque domain-work handles、bound acknowledgement port、contribution correlation、terminal report-set validation与单点 finalization；目标测试证明 skipped / not-applicable zero binding calls、zero-work applicable return，以及 `skipped | failed -> result null`、`completed -> exactly one result`。
- [x] 1.5 实现 RecordManager的 bound sink、catalog / provenance validation、location-independent identity、immediate commit、equivalent replay idempotency、arrival-neutral conflict与 canonical snapshot ordering；目标测试证明 invalid candidate使 owning run integrity failure、same-ID / different-body阻止 publication、此前独立有效 records保留，且 secret / location / message / arrival / checkRunId不进入 stable identity。
- [x] 1.6 实现 direct-runner private adapter和完整 frozen contribution-batch coordinator；以 synthetic runner覆盖 legal `passed | failed`、illegal `not-applicable` candidate、unavailable、throw / rejection、其它 invalid result、ack violation、missing / duplicate / unknown report及 records-after-failure组合。通过 foundation contract tests、局部 diff审阅和无 consumer补偿层检查后，记录 checkpoint 1放行证据。
- [x] 1.7 完成 policy / reference contract lock：先以 target tests固定 named-reference input / identity、per-Check reference evidence status、comparison relation envelope、normalized policy grammar、acceptance -> views -> ordered readiness -> boolean `blockWhen`顺序、typed evidence-ref grammar / ordering、GateResult shapes / reason codes、human quality / verification status projection、current `all | changed | regressions` mapping及 missing / invalid / unavailable reference行为；证明 current policies通过自己的 readiness阻止 failed-run retained records形成 evaluated gate并以 canonical record refs保留 blocking order，另一个受控 policy可把相同 failure status当普通 `blockWhen` operand，`--verification-output`不改变 Core / publication / outcome，reference evidence不伪造第二个 public CheckRun，也不把可信 current facts重写为 execution failure。
- [x] 1.8 将 `file-metrics` 与 `file-code-lines` 迁移为首个真实纵向路径，从 current / baseline approved exact inputs经 private binding、source-scope batch acceptance、Check / Record managers和 reference evidence到达 in-memory final Core snapshot；运行 scanner-backed target tests与局部 diff审阅，证明越界 batch zero partial conversion、earlier-record retention且没有 public flag、output双写或 compatibility layer后，记录 checkpoint 2放行证据。
- [x] 1.9 将 function metrics与 duplicate detection迁移到 `function-metrics`、`duplicate-detection`及其四个 record types，并实现 closed named `DecisionPolicy` evaluator、explicit reference handoff与 current config / CLI单向 adapter；exhaustive证明 legacy `acceptedWarnings[].checkId`只映射为 owning Check +同名 `recordTypeId` selector、legacy filters只使用 catalog允许的 predicates、three gate names只选择 ordinary policies。分别保持 scanner-private payload / failure、current / baseline source scope、jscpd cache/backend identity与 acceptance语义；通过 built-in / policy / comparison-incomplete目标测试后，记录 checkpoint 3放行证据。
- [x] 1.10 完成 publication contract lock：在 mapper / serializer / consumer迁移前，用 runtime schemas、schema-derived DTO及 serializer / validator tests固定 machine-v2 schema / instance identities、exact fields / nullability / enums、canonical arrays、JSON / NDJSON byte grammar、two-file set invariants、prior-v2 / stale-v1 / report cleanup、owned temp lifecycle、report / console labels与 warning / accepted-record previews、annotation input及 error / process-exit mapping；生成并独立验证 canonical schema / example candidates。
- [x] 1.11 从一个 validated publication model一次性迁移 publication、`run.json`、`records.ndjson`、`report.md`、console、annotation、CLI outcome和 dogfood；保持 Core / decision / machine candidate-set validation先于 canonical writes、machine / report same-directory temp publication、artifact-work handled failure cleanup、pre-work zero output I/O与 current-run evidence边界，并证明 `raw/**`仍是 machine set之外且可部分保留的 scanner-private material。
- [x] 1.12 删除 capability / completeness、warning / channel、machine-v1 mapper / schema / validator / reader / writer与 dual-consumer可达路径；清理 selected artifact directory中的 retired canonical names，更新 public shallow exports、Architecture / Quality / Output / CLI / Config / Scan Scope / Scanner owners、schemas / examples、fixtures和语义 Case catalog，并用迁移矩阵与 scoped legacy search核对没有遗漏 consumer，完成 checkpoint 4 public hard cut。

## Verification

- [x] 2.1 重跑 foundation model / validator / manager / coordinator目标测试，证明 catalog / binding一对一、run / result合法矩阵、coverage / acknowledgement、record identity / conflict / retention、terminal-report cardinality和 deterministic primary diagnostic均符合 foundation contract lock。
- [x] 2.2 重跑 `file-metrics` current / baseline exact-input、source-scope / batch rejection、reference evidence与 in-memory snapshot目标测试，证明 walking skeleton没有 public dual path且 checkpoint 2假设仍成立。
- [x] 2.3 重跑 function / duplicate built-in、jscpd cache、policy validation / evaluation、acceptance、ordered readiness、named reference、comparison relation / incomplete evidence与 three gate intent目标测试，证明三个 producing Checks保持各自领域 / adapter边界、current policies保持既有 not-evaluated语义且 Core不增加 global completeness / comparison reducer。
- [x] 2.4 重跑 machine set、schema / example、serialization / validation、prior-v2 / stale-v1 / report / temp cleanup、pre-work zero-I/O、report、console quality / verification status、`--verification-output` parity、CLI outcome / exit、annotation与 dogfood consumer目标测试，证明 deterministic bytes、同源 projection、preview-only flag、handled failure precedence和 single-active public hard cut。
- [x] 2.5 在全部测试 / Case修改后运行 `bun run test-evidence:check`；运行 product runtime import boundary、`bun run typecheck:product`、`bun run lint:product`、`bun run test:product`及正式 CLI help / controlled scan smoke，确认测试实体 / 语义 Cases双向闭合且 runtime只由 `src/product/**`与声明依赖闭合。
- [x] 2.6 运行 `bun run decisions:check`、`bun run validate`、`bun run change-plan -- check changes/establish-check-record-core`、`bun run verify:vibe-check-workspace:required`、`bun run verify:vibe-check-workspace:full`与 full dogfood；检查 stable owners、runtime schemas、published materials、全部 surfaces和迁移矩阵一致，旧 IDs / paths / readers不可达且最终 diff只覆盖本 Change owners。
