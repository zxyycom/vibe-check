# Tasks

任务按 Decision → façade实现与语义证据 → current owner → 性能与Gate 的顺序推进；任何一步需要修改source-aligned core时停止。

## Readiness

- [x] 0.1 已用正式Investigation恢复fixed Lizard 1.24 baseline、B scope、reader-dispatch profile、Python cache和探索性候选收益。
- [x] 0.2 已建立`active + unaligned` reader-resolution Decision，并确认private-port、provenance/deviation与Product owner边界。
- [x] 0.3 已运行Test Evidence起点全树检查，确认454个current Bun entities全部映射到112个Cases。

## Implementation

- [x] 1.1 在手写`port-facade.ts`实现source-order-derived private index和共享resolver，不修改source-aligned registry/core/readers/shared/extensions。
- [x] 1.2 扩展façade differential/property tests，覆盖fast/fallback、55/56 mapping、precedence、case、Unicode、line terminator、path与unknown边界，并同步现有semantic Case。
- [x] 1.3 更新current deviation与functionMetrics/scanner owner，只记录实际形成的私有host seam和边界，不建立public算法承诺。
- [x] 1.4 以相同representative manifest形成15-block ABBA before/after raw evidence和output equality；若无稳定收益则撤销实现。

## Verification

- [x] 2.1 运行façade、registry、adapter、oracle、malformed、source identity和extension protocol最窄tests，以及Test Evidence全树闭合。
- [x] 2.2 运行typecheck、lint、dependency/import/public-surface与Investigation/Decision/Change/docs验证，确认translated body与package surface零漂移。
- [x] 2.3 派发correctness-only reviewer检查filename等价、fallback、source fidelity和非机械差异，不做风格偏好审查。
- [x] 2.4 派发最终优化代理分别做AI-ready文档和普通项目编码规范审阅，只处理可证问题且不优化translated core。
- [x] 2.5 运行`verify:vibe-check-workspace:required`与`verify:vibe-check-workspace:full`，按实际证据核对Decision alignment和Change成功标准。
