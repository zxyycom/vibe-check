# Source-alignment deviations

This ledger records every identified non-mechanical difference from Lizard 1.23.0 source ranges. It is not permission to simplify an in-range analyzer module. The companion provenance ledger carries fixed-tag or fixed-package SHA-256, inclusive source lines, implementation evidence, and required verification. A translated target satisfies no Readiness or hard-cut claim until its stated direct verification has actually passed.

| Source range                                                                                            | Product boundary or deviation                                                                                                                                                                                                          | Why it differs                                                                                                                                                                                                                                                                                                                                          | Required evidence before enablement / hard cut                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `lizard.py` lines 91-98, 607-620, 623-625, 681-788, 1012-1022, 1155-1158 | In-memory analyzer and extension lifecycle are retained | `analyze_files` retains FileAnalyzer construction and ordered cross-file reassignment; `map_files_to_analyzer` retains its source-level mapping handoff after the Product supplies admitted in-memory source units. Python file discovery, decoding, reader selection and multiprocessing stay outside. `OutputScheme` retains its class state and pure Python-compatible string-format methods, while report invocation remains excluded. | Core/protocol lifecycle tests, OutputScheme Unicode/strip tests, source-field/schema tests, and Check integration evidence. |
| `lizard.py` lines 1081-1102                                                                             | Python `import_module` name load becomes a closed internal registry                                                                                                                                                                    | Product cannot dynamically load arbitrary Python modules or expose a plugin surface. Known deferred names throw `DeferredExtensionBodyError`; directly supplied internal instances/classes retain ordered insertion semantics.                                                                                                                          | Core/protocol tests for default processor order, ordered insertion, direct registration, and known/deferred/unknown name failure.                                                                                                                                                                                                                                                                                                    |
| `lizard_ext/extension_base.py` lines 1-12                                                               | Python dynamic state is represented by typed `ExtensionBase`, explicit context assignment and `Iterable`/generator members                                                                                                             | TypeScript must make protocol members, nullability and lazy iterable boundaries explicit. The token-state lifecycle and context handoff remain in-range.                                                                                                                                                                                                | Extension-base lifecycle/context-handoff and lazy hook-order tests.                                                                                                                                                                                                                                                                                                                                                                  |
| `lizard_ext` 19 named concrete bodies, exact lines in provenance ledger                                 | Explicit deferred-name error; not registered by default                                                                                                                                                                                | They are optional Lizard extension bodies outside the first Product measurement contract. Their protocol, ordering and error boundary remain in-range.                                                                                                                                                                                                  | Separate Change with source-range ledger, fidelity review, corpus and public-contract review.                                                                                                                                                                                                                                                                                                                                        |
| `lizard_ext/default_ordered_dict.py` and `lizard_ext/keywords.py`                                       | Deferred concrete-extension implementation dependencies                                                                                                                                                                                | They are used only by deferred `lizardduplicate.py` and `lizardwordcount.py` respectively, so they remain deferred with those bodies rather than becoming internal protocol.                                                                                                                                                                            | Adopt with the owning extension body in a source-range and fidelity-reviewed Change.                                                                                                                                                                                                                                                                                                                                                 |
| `lizard_languages/__init__.py` lines 1-67                                                               | Closed analyzer-internal ordered reader registry with an existing host-spelled `getReaderFor` delegate                                                                                                                                 | Python package import/discovery becomes a finite TypeScript registry; it retains source reader order and suffix dispatch, while Product admission and fallback remain outside. The source-named `get_reader_for` owns the lookup; the existing internal Product caller spelling delegates to it and does not create a plugin/public scanner API. | `reader-registry.test.ts` proves 27-reader source order, 55-suffix case-insensitive selection and the explicit unknown-suffix result; the source-identity AST test closes the source-named registry definition.                                                                                                                                                                                                                                                                                                |
| `lizard_languages/erlang.py` lines 5-8, 36-42; Pygments 2.18.0 `pygments/lexers/erlang.py` lines 22-146 | Reader-local, source-derived Erlang lexer replaces the Python/Pygments runtime                                                                                                                                                         | Lizard delegates token classification to Pygments. Product cannot ship that Python runtime or Pygments generic lexer framework, so `erlang.ts` directly carries the covered ErlangLexer rules in source order without exposing a reusable lexer API. The Pygments source is an additional BSD-2-Clause provenance, not an excluded host implementation. | Verified Pygments wheel/source/range/hash and the dual MIT/BSD derived-source header. `bun test src/package-checks/function-metrics/analyzer/readers/erlang.test.ts` passes four fixed-tag suffix fixtures, source/adversarial metric observations, all seven Pygments 2.18 rule token-and-metrics cases, and the exact lexer probe. Package legal materials, Product integration and hard-cut evidence remain separate obligations. |
| `lizard_languages/swift.py` lines 10-42 and `lizard_languages/kotlin.py` lines 5-25                     | Python multiple inheritance becomes named `SwiftReplaceLabel` composition                                                                                                                                                              | Both readers require CodeReader, C/C++ comments and the shared label transform. TypeScript composes that one named transform at each reader boundary, preserving the source preprocessing order and in-place label rewrite without emulating Python MRO.                                                                                                | `golike-readers.test.ts` exercises Swift/Kotlin upstream scenarios and backtick tokens; its transform cases verify the same source-order label rewrite before reader state processing.                                                                                                                                                                                                                                               |
| `lizard_ext/__init__.py` lines 1-31 and Lizard CLI/output/version modules                               | Excluded entry surface                                                                                                                                                                                                                 | They provide extension-result, command/file/output/version surfaces replaced by Product final-data and package owners.                                                                                                                                                                                                                                  | Product final-data/no-process/no-public-plugin evidence.                                                                                                                                                                                                                                                                                                                                                                             |
| `lizard_ext/auto_open.py` lines 1-36                                                                    | Product admitted-byte decoder mirrors `auto_read`: strict input uses `utf-8-sig` plus universal newlines; strict UTF-8 failure retries original bytes with `errors="ignore"` and raw newlines                                          | Path opening/encoding is an excluded command-side entry surface, not an in-memory analyzer dependency. The Product adapter nevertheless owns its required replacement policy; this is a narrow Bun-host adjustment, not a reusable decoder framework.                                                                                                   | Direct fixed-source `auto_read` differential observations cover valid BOM, legal U+FFFD, invalid byte removal, truncated multibyte removal, BOM retention on fallback, and strict/fallback newline behavior; `measurement.encoding.test.ts` reproduces them, and a deterministic 271-sequence differential run matched fixed source.                                                                                                 |
| Python regex fragments in `lizard_languages/code_reader.py` and readers                                 | `normalizePythonRegex`/inline-flag adaptation produces JavaScript Unicode-compatible patterns                                                                                                                                          | Python accepts escapes/flags JavaScript Unicode regex rejects; the adaptation is narrow and must not rewrite token semantics beyond those host incompatibilities.                                                                                                                                                                                       | Tokenizer corpus for escaped punctuation, inline flags, macro/comment/string/symbol boundaries.                                                                                                                                                                                                                                                                                                                                      |
| Python `splitlines()` / `isspace()` / `str.strip()` behavior in processor and reader preprocessing      | TypeScript uses an explicit Python-whitespace predicate: `\p{White_Space}` plus U+001C–U+001F, excluding BOM; line splitting explicitly recognizes U+2028; comment directives apply a code-point-safe Python `str.strip()` equivalent. | JavaScript's whitespace and line APIs differ from Python. The predicate, splitter, and directive trim preserve Python-compatible token/line distinctions, comment-inserted line counts, and newline-token behavior rather than accepting host defaults.                                                                                                 | Core/tokenizer U+001C–U+001F, BOM and U+2028 differential tests, including comment-directive U+001C forgiveness and BOM non-forgiveness, plus reader indentation/comment corpus.                                                                                                                                                                                                                                                     |
| `lizard_languages/code_reader.py` lines 1-238 and concrete reader class attributes in the translated `lizard_languages/**` ranges | Fixed TypeScript host casing uses one storage slot with a source-spelled internal alias | `generateTokens`, `languageNames`, and the condition-category stores retain the existing typed TypeScript host surface; `generate_tokens`, `language_names`, and source category names resolve to that same storage. `languages` remains its own source field and is not conflated with `language_names`. The aliases are analyzer-internal and do not create a Product plugin/model API. | CodeReader source-field/single-storage and protocol metadata tests; the checked-in source-identity manifest and AST test close every translated reader/shared range. |
| `lizard_languages/clike.py` lines 151-220 and `lizard_languages/ttcn.py` lines 34-37 | Python class attributes `parameter_bracket_open` / `parameter_bracket_close` become TypeScript static storage with same-named instance lookup | Python resolves each state instance through a class attribute and lets `TTCNStates` override it. TypeScript keeps one static value per class plus a same-named instance getter that resolves the runtime state constructor; this preserves source override lookup without a parser framework or duplicate mutable state. | `shared/clike.test.ts` source override cases, C-like/TTCN fixture oracle tests, and the source-identity AST test. |
| `lizard_languages/zig.py` lines 28-31 | `_type_definition = GoLikeStates._state_global` is a source-spelled receiver-bound state callback | Python stores an unbound parent method as a class attribute and binds it on instance access. The TypeScript state dispatcher invokes callbacks with the owning receiver, so the same source-spelled callback is stored once on the state instance and delegates to `super._state_global`. | Go-like/Zig fixture oracle tests and the source-identity AST test name the call-signature host seam. |
| `lizard_languages/ruby.py` lines 10-18 | `MyToken(str)` becomes a constructible typed token object | TypeScript cannot model Python `str.__new__` plus a mutable `begin` field. The source token's string value and start offset are retained by one object used only by Ruby tokenization; no generic token framework is introduced. | Ruby normal/edge/malformed fixture oracle tests and the source-identity AST test name the string-subclass host seam. |
| `lizard_languages/code_reader.py` lines 48-56 and `lizard_ext/extension_base.py` lines 8-12 | `CodeStateMachine` token invocation is named `consume`; `CodeReader` and `ExtensionBase` retain their distinct iterable `__call__` forms | One TypeScript inheritance member cannot simultaneously model the source token-state `__call__(token)` return and ExtensionBase's iterable processor `__call__(tokens, reader)` with a compatible override signature. The translated state machine keeps the same fields, transition/callback order and source aliases; internal callers use `consume`. | CodeReader completed-state/source-field/decorator tests and ExtensionBase context/state-handoff test. |
| Python descriptor hook lookup (`set_args`, `cross_file_process`, `print_result`)                        | Instance-first, then constructor-static resolver                                                                                                                                                                                       | Python descriptors can supply a hook from an instance or class. TypeScript resolves those two internal locations in that order, without dynamic module loading or a public plugin API.                                                                                                                                                                  | Protocol instance/static wordcount-style hook, set-args, cross-file, print-result and silent-order tests.                                                                                                                                                                                                                                                                                                                            |
| Analyzer cancellation                                                                                   | No cancellation hook exists in the upstream analyzer core/protocol ranges                                                                                                                                                              | A Product cancellation boundary belongs at Check-owned execution without changing token/processor semantics.                                                                                                                                                                                                                                            | Exact abort timing, input-limit and result/failure mapping evidence.                                                                                                                                                                                                                                                                                                                                                                 |
