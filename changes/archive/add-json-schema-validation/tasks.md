# Tasks

任务复用已落地 strict JSON helper，并按已确认的 identity、controlled-reference、issue-cap 与 output
contract，完成 registry/binding、2020-12 engine、safe Records 与首版 package 证据。

## Readiness

- [x] 0.1 已按当前 ordinary Check/options/Record/scope contract 重建范围，并移除旧 shared policy、TaskPlan、comparison/cache 与 Record catalog 前置。
- [x] 0.2 已固定显式 registry/binding、strict document reuse、safe authoring identity、四类 issue 与首版非目标。
- [x] 0.3 已以当前 strict JSON helper、其 owner evidence 与 archived `add-json-validation` 为基线运行 Test Evidence 起点检查，并审计 Ajv 2020-12 entry、Bun compatibility、installed-runtime move、license/material、reference behavior 与 credential redaction 边界。
- [x] 0.4 已记录 Check-level identity policy：默认 require-match、configuration-authoritative 与 document-authoritative 三种全局模式；local/bundled/allowlisted HTTPS resolution，默认 offline，禁止 `$dynamicRef` 与非授权 external reference。
- [x] 0.5 已记录 closed authoring grammar/failure mapping、固定 private 100-Record cap、truthful truncation metadata、Definition-time rejection、scope escape、empty bindings 与 unbound schema 的 terminal behavior；受控网络长期方向已按 Decision workflow 保存。

## Implementation

- [x] 1.1 扩展 strict helper 的 valid branch，返回私有 frozen JSON value；保持 `jsonValidation` verdict，并补 document boundary tests。
- [x] 1.2 新增闭合 authoring/options validation、immutable registry/binding normalization 与 exact global-scope work plan；覆盖 ID/path/source grammar、uniqueness、empty/unbound/scope behavior。
- [x] 1.3 实现 private async 2020-12 Ajv adapter：三种 identity mode、local/bundled resolver、allowlisted HTTPS source、fixed timeout/byte/concurrency/de-duplication、strict remote document 与 safe error normalization。
- [x] 1.4 新增 `jsonSchemaValidation` value、`JsonSchemaValidationOptions`、stable Records、fixed final-data counts 与四态 settlement；覆盖 document/compile/reference/keyword/blocked binding/issue-cap/redaction。
- [x] 1.5 把 Ajv 移为 production dependency，并同步 public exports/contract、owner docs、README/JSDoc/examples、package license/material、semantic Cases 与 isolated candidate consumer。

## Verification

- [x] 2.1 运行 document/options/identity/ref/HTTPS-policy/keyword/scope/issue-cap/credential-canary 最窄 tests，以及 `bun run test-evidence -- check --root .`。
- [x] 2.2 运行 product typecheck、lint、tests、docs/package candidate 与 installed Bun acceptance。
- [x] 2.3 运行 Decision/Change checks、required/full Gate，复核 artifacts/logs/errors 不含 raw schema/instance bytes、URI credentials、absolute paths 或 engine-native messages。
