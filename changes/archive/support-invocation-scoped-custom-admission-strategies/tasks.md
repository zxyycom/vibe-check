# Tasks

按基线恢复、contract implementation、行为证据和 workspace verification 推进；每个 checkbox 只在对应产物可复核后勾选。

## Readiness

- [x] 0.1 读取 `docs/architecture.md#private-admission-strategy-lifecycle`、`docs/configuration.md`、`docs/api-mechanics.md#outputs-与-runresult-边界`、相邻 source/tests 与两个 active + unaligned Decisions，建立 current implementation / Plan contract 对照，并确认 implementation worktree 包含 stable private lifecycle seam。Owner: architecture/configuration/API mechanics + invocation/scheduler。
- [x] 0.2 通过最窄 test-evidence 查询定位 custom admission、terminal Hook、private lifecycle 与 installed consumer 的相关 Case/实体；如需修改 native test 正文、节点或 Case prose，按 `test-evidence-review` 在改动前运行 `bun run test-evidence -- check --root .`。Owner: testing/case-maintenance。
- [x] 0.3 建立 implementation matrix：simple synchronous `decide`、prepared asynchronous `prepare`/Run-local `decide`/optional `complete`、public compatibility acceptance、strategy-kind fingerprint、trusted-host context、normal/cancel/policy-fault/preparation-failure/pre-terminal-engine-failure/terminal-side-effect-failure/overlap。为每格指定 public declaration、runtime test、Case 和 output assertion：generic Hook list 非空或 successful prepared result 含 `complete` 时 enabled；两者皆无时 disabled；enabled Run 未形成 sealed sequence 时 `not-run`；actual participants 全部成功时 `succeeded`，任一 generic/complete failure 时 `failed`。特别覆盖 prepare failure 的有/无 generic Hooks，以及无 generic Hooks 的 prepared result 有/无 `complete` 与 later no-context。Owner: project-definition + invocation/terminal delivery + Test Evidence。
- [x] 0.4 按 `docs/change-execution-order.md` 核对 stable private seam 是唯一 hard prerequisite，并记录 Simulation 与 algorithm Change 的 owner 冲突和推荐串行合入顺序；后续事实改变 decision context 或 terminal owner 时更新 matrix 并重跑相应验证。Owner: Change coordination。

## Implementation

- [x] 1.1 在 `src/project-definition/scheduler-policy.ts` 与 public exports 定义 closed custom grammar：`strategy.kind: "simple"` 提供 synchronous `decide`，`strategy.kind: "prepared"` 提供 async-capable `prepare` 并返回 per-Run `decide` 与 optional async-capable `complete`；三个 callback context 分别使用 frozen graph-ready、current decision 与 sealed terminal DTO。Owner: public scheduler-policy contract。
- [x] 1.2 更新 Project Definition exact validation、normalization、declarative snapshot/fingerprint 和 declaration projection，使 simple/prepared 成为唯一 public authoring forms；strategy snapshot 只记录 kind，callback identity/source/closure 留在 runtime state。以同一 acceptance 覆盖旧 `proposeAdmission`、unknown fields、async/thenable `decide` 与 malformed prepared result。Owner: project-definition/type declarations/fingerprint。
- [x] 1.3 重构 provider/Invocation adapter：每个 graph-ready Invocation 建立 simple Run-local closure 或 await 一次 prepared `prepare`，并将 returned `decide` 作为唯一 frozen Scheduler policy handoff；将 prepare throw/rejection 映射为 `admission-strategy-preparation-failed`，并证明 overlapping Runs 持有隔离 state。Owner: `src/project-run/admission-strategy-provider/**` + `invocation.ts`。
- [x] 1.4 将 Scheduler-facing `decide` 保持为 synchronous result-only seam，并复用 existing hard guards 和 policy-fault drain behavior；为 seal 后的 policy fault 连接该 Run 的 completion delivery。Scheduler API 只承接 policy selection。Owner: task-scheduler adapter/decision owner。
- [x] 1.5 保留 Scheduler 在 sealed context 上的 existing `internal summary → configured generic Hooks（全数有机会）` runner；Invocation/orchestration 在该 runner 返回后交付 prepared `complete`，并形成 `Scheduler runner → prepared complete → aggregate` 的 single semantic pipeline。Owner: invocation + scheduler-terminal-measurement。
- [x] 1.6 扩展既有 `outputs.measurementHooks` / `scheduler-measurement-hooks-failed` aggregate participation：output tracker 在 successful `prepare` 返回含 `complete` 的 result 后启用 field；generic Hook list 非空同样启用。两者均无时保持 disabled，已启用但无 sealed sequence 时为 `not-run`，terminal participants 的 settlement 决定 `succeeded`/`failed`。结果 mapping 保留 sealed Task/Check snapshot、selected aggregation、summary containment 与 primary-result precedence。Owner: RunResult/API mechanics/types owner。
- [x] 1.7 更新 Configuration、Architecture、API mechanics、README/package guides、public JSDoc 与 examples：实施前清晰标注 current fact 和 Plan contract，落地后以 implementation fact 更新；说明两种 authoring form、trusted-host context、terminal sequence、existing output field 和 failure matrix。Owner: corresponding stable docs + package documentation tooling。
- [x] 1.8 更新 `scripts/package/candidate/external-consumer/type-acceptance.ts` 及 public inventory/QuickInfo assertions，使 installed consumer 编译 simple/prepared form、prepare/complete context 和 strategy-kind exports，并以 `@ts-expect-error` 验收 public compatibility boundary 与 context shape。Owner: package candidate/public declarations。
- [x] 1.9 新增或修改最窄 runtime behavior tests，覆盖 prepare once/async、overlap isolation、same-context synchronous decide、prepare failure、policy-fault drain 后 completion、pre-terminal engine failure、Scheduler generic Hook continuation、complete aggregate failure、normal/primary precedence、frozen contexts 与 fingerprint/normalization；逐一证明 disabled、enabled/`not-run`、`succeeded`、`failed`，包括 prepare failure 的有/无 generic Hooks、prepared result 的有/无 `complete` 与 later no-context。Owner: invocation/task-scheduler/project-definition tests。
- [x] 1.10 维护 `docs/testing/cases/quality-runtime.md` 与相关 Case entity mapping，使 Case 分别证明 public grammar、hard guards、terminal delivery/failure 和 installed-consumer surface；按需要调整 Case 拆分/归并并保持实体 closure。Owner: Test Evidence。

## Verification

- [x] 2.1 运行受影响的 project-definition、fingerprint、custom-admission-policy、terminal-measurement、invocation lifecycle 与 package candidate tests；逐格核对 0.3 matrix，确认 lifecycle ownership、sealed facts、existing output name/diagnostic 和 public compatibility acceptance。
- [x] 2.2 在 tests/Cases 改动前后运行 `bun run test-evidence -- check --root .`，复核 Owner/Proves 与 native Bun registration closure，并运行新增或修改节点的最窄目标测试。
- [x] 2.3 构建并验证 installed package candidate，运行 external-consumer type acceptance 与 runtime evidence，确认发布 declaration 独立提供两种 strategy form 和 compatibility acceptance。
- [x] 2.4 运行 `bun run validate -- docs`、受影响 public documentation/declaration projection 检查、`bun run typecheck`、`bun run lint` 与目标 `bun run test`；审阅 diff，确认 declared machine v4 scope、Simulation 和 algorithm Change 的 owner boundary 保持不变。
- [x] 2.5 运行 `bun run verify:vibe-check-workspace:required` 和 `bun run verify:vibe-check-workspace:full`，记录每个失败或未运行项及其对 public contract 的影响；两项通过后才可宣称跨 owner implementation evidence 闭合。
- [x] 2.6 当 stable owner docs、TypeScript definition/exports、runtime/tests、Test Evidence、installed consumer 与 required/full evidence 都成为 current fact 后，按 `decision-records` 核对并分别使用 `bun run decisions -- mark-aligned adopt-invocation-scoped-custom-admission-strategy-authoring.md` 与 `bun run decisions -- mark-aligned extend-measurement-hook-output-to-prepared-complete.md`；此前保持两份 Decision `active + unaligned`。
