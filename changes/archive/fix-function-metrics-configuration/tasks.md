# Tasks

任务先固定长期方向与 public contract，再实施 area measurement 和 outcome policy，最后同步证据并完成分层验证。

## Readiness

- [x] 0.1 恢复 configuration、scanner dependency、scan scope、quality metrics、测试策略与当前 function/Lizard 实现。
- [x] 0.2 对照 duplicate-detection Change，确认复用 constructor/adapter/strict-policy原则而不建立 generic scanner或同步改造 file metrics。
- [x] 0.3 获得用户对全局 finding policy、area override和完整 evidence不短路方向的确认，并记录当前工作区并行改动边界。

## Implementation

- [x] 1.1 建立并激活 function configuration 与 Lizard adapter protocol 的长期决策。
- [x] 1.2 实现 closed defaulted constructor、resolved options、正安全整数/allowance/area/scanner validation。
- [x] 1.3 实现 area-owned exact-input union、重叠 area strict limits、blocking aggregation、Record 与 final data。
- [x] 1.4 将 Lizard version/CSV arguments移入 adapter，删除 public argument passthrough并更新 fake executable tests。
- [x] 1.5 同步 public exports、repository Definition、package docs/API mechanics、稳定 owner 与 Lizard port Change。
- [x] 1.6 更新直接测试与语义 Case 账本，审阅 constructor、scope、blocking evidence和adapter failure证明。
- [x] 1.7 按完整编码规范移除 resolved options 中未消费的顶层 policy，收紧 area map、version、signal 与稳定排序边界。
- [x] 1.8 按 AI-ready 文档规范校准 authored/resolved policy、默认值、结果与 adapter-owned 参数说明。

## Verification

- [x] 2.1 运行 function-metrics/Lizard 与 package documentation 最窄测试。
- [x] 2.2 运行 Test Evidence、typecheck、lint、format、docs、decision和Change检查。
- [x] 2.3 运行 `verify:vibe-check-workspace:required` 并审阅目标 diff和现有并行改动边界。
- [x] 2.4 重建并验证 package candidate，运行 full workspace verification和repository quality，核对新 public API与blocking/non-blocking结果。
- [x] 2.5 对齐长期决策并完成 Change 成功标准语义审阅；没有明确归档授权时保持 active Plan。
- [x] 2.6 重新运行目标测试、Test Evidence、required/full Gate 与 candidate package 验证。
