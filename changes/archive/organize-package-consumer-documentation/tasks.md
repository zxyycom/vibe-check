# Tasks

任务先闭合长期结构和当前证据，再重构内容、投影与 package acceptance，最后验证 exact candidate。

## Readiness

- [x] 0.1 核对 package 现有文档 inventory、生成 owner、artifact/installed acceptance 和 Test Evidence 起点。
- [x] 0.2 记录用户确认的单 README 分级方向，并确认与当前中文说明和可读 package decisions 一致。
- [x] 0.3 建立编码型 AI 的实际消费闭包、代表性任务与可观察成功条件，并回查 public API 与 invocation owner。
- [x] 0.4 审计 README、API mechanics 与七项指南的负向描述比例、正文重心、重复 owner 和内部迁移残留。

## Implementation

- [x] 1.1 重写 README template，并生成以自定义 API、效果和随包 Check 直链为主的 root README。
- [x] 1.2 新增唯一 API 机制 template/output，迁入进阶 preflight、dependency、Controls、outputs 和结果边界。
- [x] 1.3 删除 Check index，更新七项 Check 指南的共用机制链接并保持 exact inventory。
- [x] 1.4 泛化 Markdown example projection，更新 documentation collector、CLI、fingerprint 与 package audits。
- [x] 1.5 更新目标 tests、Repository Tooling Cases 和稳定脚本 owner 文档。
- [x] 1.6 校准 README 的核心术语、最短使用顺序、canonical data 边界和 Run / Check 结果判读，并替换不能证明实际效果的 quick-start。
- [x] 1.7 校准唯一 API mechanics 文档的 `defineConfig` / `run` 责任、生命周期总览、条件和完整结果分支，并让进阶示例证明 `completed` 与失败 Check 可以同时成立。
- [x] 1.8 审阅七项 Check 指南的局部自足性，统一指向通用 preflight 与结果 owner，移除要求 consumer 猜测的内部措辞。
- [x] 1.9 将 README 收敛为自定义 Check 的正向执行主线，把发布、surface 与 package layout 边界降级到末尾。
- [x] 1.10 将 API mechanics 收敛为 invocation lifecycle，删除内部模块枚举和重复否定，保留可判别失败与安全条件。
- [x] 1.11 将七项 Check 指南改为适用范围、options、效果与安全边界，去除重复 Run 结论、内部 owner 和否定式非目标清单。
- [x] 1.12 以 AI-ready 文档规范复核公开导出类别、术语、默认值、authoring/resolved 边界和 exact guide inventory。

## Verification

- [x] 2.1 运行 package API documentation 目标 tests、投影 write/check、docs validation 和 Test Evidence closure。
- [x] 2.2 重建 package candidate，检查最终文档路径、内部链接、tar/installed material 和 external examples。
- [x] 2.3 运行 Decision/Change checks、scripts typecheck/lint/format 以及 full workspace package verification。
- [x] 2.4 重新生成 package 文档，以自定义 Check、失败 Check aggregation 和随包 Check 配置三项代表性消费任务做语义恢复审阅，并重跑完整 candidate verification。
- [x] 2.5 重新生成并审阅最终 package material，确认主线、必要负向边界和迁移残留收敛后，重跑文档、candidate 与 full Gate 证据。
- [x] 2.6 重新构建本次审查后的 exact candidate，并复跑 installed documentation、examples、types、runtime 与 full Gate。
