# Tasks

本任务按 owner 迁移、调用链同步和分层验证的顺序完成，并保留可交接的事实证据。

## Readiness
- [x] 0.1 审计 AGENTS、脚本 owner、active Decisions、原 Foundation callers、Gate checks、package/candidate imports 与 Test Evidence 起点。
- [x] 0.2 确认 docs provider/validation acceptance、quality/Gate 层级与 required/full 边界保持不变。

## Implementation
- [x] 1.1 迁移原 Foundation 的存活 process、repository、diagnostic/value capability，删除无生产消费者文件并更新 node:test evidence/Cases。
- [x] 1.2 将 docs validation workflow 和 validators 重组到 validation/documentation，保留 docs providers 与 workspace root。
- [x] 1.3 删除 Gate checks 容器，将 execution mapping 与具体 Check 重组到明确 owner。
- [x] 1.4 将 package public contract、cross-lifecycle capabilities、projection registries 和 artifact audit 归入正确 package owner。
- [x] 1.5 对齐 quality scripts coverage、candidate fingerprint 说明、layout characterization 和 owner documentation。
- [x] 1.6 同步受影响 Tests、semantic Cases、active Change current-path references 和 AI-ready 文档表述。

## Verification
- [x] 2.1 运行最窄 node:test / package / Gate target tests 与 Test Evidence closure。
- [x] 2.2 运行 scripts typecheck、lint、format、validate、decisions、目标 Change 与 active Changes check-all。
- [x] 2.3 运行 required workspace verification，审查 diff/status，记录结果和未覆盖边界。
