# Tasks

任务只闭合旧共享方案的 artifact 退出，不新增产品实现；全部证据成立后等待单独归档授权。

## Readiness

- [x] 0.1 已读取当前 Configuration、Scan Scope、ordinary Check/options contract 与 `use-check-owned-file-overrides` 决策，确认 shared resolver 方向已被取代。
- [x] 0.2 已核对 `src/**` 与 public declarations 没有 shared file-policy schema、resolver、provenance 或 `explain-config` 实现。

## Implementation

- [x] 1.1 重写首版与后置 Check plans，移除本 Change、旧 `src/product/**`、TaskPlan/Manager、named reference 和 shared patch 前置。
- [x] 1.2 更新 Active Change Portfolio、发布 Draft 与交付导航，明确本 Change 已完成退出且不阻塞首版。

## Verification

- [x] 2.1 搜索 active artifacts，确认没有其它 Change 仍把 `add-file-policy-overrides` 或 Product-wide file policy 当作实施依赖。
- [x] 2.2 运行本 Change、active collection、Decision 与 required workspace checks；确认没有产品代码或 public contract 变化。
