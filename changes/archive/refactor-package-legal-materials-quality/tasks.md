# Tasks

本 Plan 依次确认现有法律闭包，实施私有模块边界，并以 direct 与 package artifact 证据验证同一 Outcome。

## Readiness

- [x] 0.1 已读取 package artifact owner、编码规范、相邻法律审计及 staging/tar/installed callers，确认固定 identity、七阶段顺序、错误边界和不变范围。
- [x] 0.2 已确认既有 package artifact material Case 可承接 direct 审计错误顺序实体，并完成其映射更新。

## Implementation

- [x] 1.1 已将 fixed Lizard/Pygments provenance inventory、entry 与 supplemental parser，以及 translated-target closure 提取为具名私有模块，保持既有校验与错误文本。
- [x] 1.2 已将 package-local legal byte/header/untracked/deferred/notice audit 提取为具名私有模块，并保留 orchestrator 的显式七阶段顺序和 caller 边界。
- [x] 1.3 已添加最小 direct mutation/error-order test，并将实体并入既有 package artifact material Case。

## Verification

- [x] 2.1 direct legal-materials 与 artifact staging/tar acceptance 共报告 6 个通过测试；它们分别覆盖七阶段首错顺序和真实 package artifact 路径。
- [x] 2.2 focused quality 从 35 降至 31；correctness review 无阻断；Test Evidence check 报告 538/538；一次默认 Project Gate 以 31 passed、5 not-applicable、0 failed/unavailable 通过，仍为 31 条 advisory Records。
- [x] 2.3 已在本次文档审核后运行 Change Plan check、docs validate、Test Evidence check、局部 diff check 与一次默认 Project Gate；未运行 `--all` full Gate。
