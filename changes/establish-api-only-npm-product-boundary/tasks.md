# Tasks

Readiness first consumes the completed upstream Check contract; Implementation then builds public projection, Bun host, staging and exact-tarball acceptance before deleting the retained Product CLI. Package work must not create compatibility exports while the upstream handoff is incomplete.

## Readiness

- [x] 0.1 Confirm the lasting package direction: one versioned unscoped `vibe-check`, Bun direct import, MIT, API-only entry, project-owned bound Run, deterministic candidate, and no registry publish without separate authorization.
- [x] 0.2 Re-plan against [`unify-check-authoring-and-execution`](../unify-check-authoring-and-execution/): replace every `BuiltInCheck` / `replace` / `append` / TaskPlan / operational-map assumption with one recursive Check, `defineCheck`, `inherit`, native spread, direct execution/results and Check-owned scanner options.
- [x] 0.3 Fix package inventory inputs to the upstream decision: four callable functions (`defineConfig`, `defineCheck`, `inherit`, `run`), three non-callable default Check values and the named type roots in [`expose-recursive-check-authoring-and-run-surface`](../../docs/decisions/expose-recursive-check-authoring-and-run-surface.md).
- [x] 0.4 Define exact-tarball acceptance roles and evidence: Project Definition, bound project Run, separate caller, ordinary default customization, inheritance, direct custom execution, Records/reference, Task semantics, effects, cancellation, export/declaration audit and repository-independence.
- [ ] 0.5 After the upstream Change completes, verify its final source/tests/docs/decisions and absence of conflicting active contracts; audit current package/release owners and refresh this Plan baseline before task 1.1.

## Implementation

- [ ] 1.1 Extend the current public-contract source with package name, exact function/value/type inventory, candidate version, MIT/license, Bun/platform support, prerequisites, manifest/release fields and consumer map; do not add placeholders or project file paths.
- [ ] 1.2 Before changing tests/Cases, use `test-evidence-review` to map upstream API/Task/Core evidence, package-consumer evidence and CLI-only retirement evidence.
- [ ] 1.3 Build the public entry and declarations from the upstream Product runtime; export exactly four runtime functions, three default values and the approved types, with no internal/wildcard subpaths or legacy aliases.
- [ ] 1.4 Implement the Bun host closure for filesystem, Git, subprocess, cache, progress/logging and canonical output without changing upstream Check/Run semantics or exposing host seams.
- [ ] 1.5 Close scanner prerequisite delivery through default Check options and installed-host diagnostics; remove any package dependence on repository mise, old environment precedence, workspace devDependencies or repository paths.
- [ ] 1.6 Build clean staging from authoritative sources, generating runtime, declarations, manifest, MIT materials, inventory, provenance and digest while root remains private.
- [ ] 1.7 Add staged import/dependency audits and typed unsupported-host/missing-prerequisite diagnostics; manifest has no `bin`, install script network effects or undeclared runtime dependencies.
- [ ] 1.8 Select the next valid `0.0.x` candidate from release evidence and generate precise-pin/breaking-risk notes without treating pack or repository version as registry authority.
- [ ] 1.9 Implement clean build plus `npm pack --json`, deterministic allowlist comparison and secure exact-tarball installed acceptance using the consumer path in Design.
- [ ] 1.10 After the replacement gate passes, delete the retained Product CLI migration diagnostic, tests/support and `product:cli`; keep repository commands as bound project Run adapters and synchronize stable docs/Cases/CI/release procedure.

## Verification

- [ ] 2.1 Compare current owner to manifest, entry, declarations, docs and fixtures; prove exact runtime/type inventory and absence of project-path or legacy symbols.
- [ ] 2.2 Run upstream authoring/Definition/Run/Task/Core/default Check tests against the public projection; prove package introduces no config loader, second scheduler, source binding or result reinterpretation.
- [ ] 2.3 From the exact tarball, typecheck and run recursive `Check`, standalone/nested `defineCheck`, all `inherit` states, native scanner/threshold spread and plain-object compatibility.
- [ ] 2.4 In the isolated consumer, prove Check-owned external prerequisites, default/missing/overridden executables, exact inputs, cache identity and no repository/environment-registry fallback.
- [ ] 2.5 Prove separate caller -> project Run -> Package Run behavior for direct execution, Records/reference, structured outcomes, dependency/mutex/cap, effects, failures, cancellation and concurrent invocations.
- [ ] 2.6 Repeat clean build and audit tarball allowlist, declarations, MIT files, provenance and digest; confirm no tests, secrets, cache, logs, artifacts or undeclared workspace material.
- [ ] 2.7 Run Product/package tests, typecheck, lint, `bun run test-evidence -- check --root .`, `bun run decisions -- check`, `bun run validate`, both Change checks and `bun run verify:vibe-check-workspace:required`.
- [ ] 2.8 Run `bun run verify:vibe-check-workspace:full` and candidate dogfood; record that build/pack/verify passed and no registry publish or credential access occurred.
