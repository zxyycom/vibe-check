# Tasks

任务先固定 owner 与基线，再实现 direct Checks 和 hook，最后完成跨 owner 验证；checkbox 只按实际证据勾选。

## Readiness

- [x] 0.1 恢复 current Gate、旧 quality policy、scanner binding、afterGate、active Decisions 与测试 owner，确认实现测试不能替代 repository scan。
- [x] 0.2 建立三条独立长期 Decision，分别承接 direct quality membership、invocation machine evidence 与 advisory performance monitoring；它们在实施与 stable owner/验证材料同步完成后，按 Decision Records 规则核对为 aligned。
- [x] 0.3 修改测试前运行 Test Evidence closure，确认起点为 262/262 entities 和 79 Cases。

## Implementation

- [x] 1.1 将四项原始 repository quality Checks 与原文件/area/finding policy 直接加入 required/full Gate entries，并作为 non-blocking observations 排除在 assurance aggregate 之外；不恢复父 wrapper 或短命令。
- [x] 1.2 让正式 Gate root 与 package verify child 使用 mise 锁定 toolchain，并把 SCC/Lizard 绝对 executable 显式交给 owning Check。
- [x] 1.3 实现 Gate-private performance baseline/observer并接入 default afterGate；覆盖匹配、不可比较、within/outside range 和 status-preserving behavior。
- [x] 1.4 更新 Gate/command/unit tests 与 Case/Test Evidence，使 direct quality facts、pinned entry 和 performance message 都有稳定行为证据。
- [x] 1.5 同步 script tooling、quality metrics、scanner dependencies 和 testing owner；完整实现与验证后，按 Decision Records 规则核对三条直接相关 Decision 是否可对齐，不预先标记 alignment。
- [x] 1.6 在 Gate invocation directory 启用标准 machine fact set，验证直接质量 Checks 的 final data 与 Records 可在进程结束后回读且没有 quality 专用 schema/reducer。

## Verification

- [x] 2.1 运行 Gate、package command 与 performance observer 最窄测试，完成 product/scripts typecheck、lint、format 和 diff checks。
- [x] 2.2 运行 Test Evidence、docs、Decision 与 Change checks；进行 AI-ready docs 和全局编码规范独立审计并修复阻断问题。
- [x] 2.3 在锁定环境采集标准 required/full 重复样本，写入可复核 baseline 并验证 advisory threshold；记录不匹配 workload 的边界。
- [x] 2.4 运行 required/full Project Gate，确认四项 raw quality Checks、单一 aggregate、performance observation、日志布局与最终 exit。
