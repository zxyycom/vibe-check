# Tasks

任务按“先证实 Git activity adapter 边界，再实现 ordinary value，最后闭合 public package 与 evidence”的顺序推进；只有实际完成和验证后才勾选对应 checkbox。

## Readiness

- [x] 0.1 已读取 ordinary Check、native composition、terminal messages/visibility、Check-owned execution dependency、aggregation、cancellation 与 public-package owners。
- [x] 0.2 已固定 value-only public surface、closed options、full commit/first-parent activity、due 边界、advisory/enforcing folding、empty input 与 whole-Check failure 语义。
- [ ] 0.3 在修改产品代码前运行 Test Evidence 起点检查，并建立 focused hermetic Git fixture spike：确认 SHA-1/SHA-256 ID、merge/revert/binary/rename/shallow history，以及 callback signal 与当前 private process runner 的 cancellation/drain 边界；将结果回写本 Design 的 private-adapter 实现观察，若它改变范围则先更新 Plan。

## Implementation

- [ ] 1.1 新增 hermetic Git history fixtures、measurement tests 与 private adapter：验证 full-ID/first-parent ancestry、commit count、cumulative numstat、empty input 与 whole-Check failures，不接受 partial clean data。
- [ ] 1.2 新增 `maintenanceReminders` value、`MaintenanceRemindersOptions`、complete runtime validation、validated definition fingerprint、ordered assessments/messages、versioned final data 和四态 terminal result。
- [ ] 1.3 同步 public exports/contract、Configuration/Quality/Output/process owner、README/JSDoc/examples 与 isolated Bun consumer；明确 consumer 通过 native object composition 设定 complete Git/reminder options。
- [ ] 1.4 新增或更新 semantic Cases，证明 entries 不成为 Check/Record/dependency identities、messages 不进入 machine publication、caller aggregation 保持独立；不把 empty package default 擅自扩展为 repository policy。

## Verification

- [ ] 2.1 运行 options/history/limits/messages/status/failure/fingerprint 的最窄 tests，并运行 `bun run test-evidence -- check --root .`。
- [ ] 2.2 运行受影响的 product typecheck、lint、tests、docs/package candidate 与 installed Bun consumer acceptance；验证 current docs、declarations 和 package inventory 使用同一 public contract。
- [ ] 2.3 运行 required Gate 与 full Gate；复核没有 constructor、auto-advance、worktree delta、partial clean data、未声明 public surface 或隐式 repository policy。
