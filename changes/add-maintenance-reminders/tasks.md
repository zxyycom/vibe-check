# Tasks

任务先锁定 Git activity evidence，再实现 ordinary default value、messages/final data、公共同步与首版 candidate验证。

## Readiness

- [x] 0.1 已读取当前 ordinary Check、native composition、terminal messages/visibility、Check-owned execution dependency与 aggregation owners。
- [x] 0.2 已固定 value-only public surface、closed options、full commit/first-parent metrics、due边界、advisory/enforcing folding、empty与 failure语义。
- [ ] 0.3 运行 Test Evidence起点检查，并用 focused Git fixture spike确认 SHA-1/SHA-256 ID、merge/revert/binary/rename/shallow与 cancellation命令边界。

## Implementation

- [ ] 1.1 先新增 hermetic Git history fixtures与 measurement tests，再实现 full-ID/first-parent ancestry、commit count和 cumulative numstat adapter。
- [ ] 1.2 新增 `maintenanceReminders` value、`MaintenanceRemindersOptions`、完整 runtime validation、ordered assessments/messages、final data与四态结果。
- [ ] 1.3 同步 public exports/contract、Configuration/Quality/Output/process owner、README/JSDoc/examples与 isolated Bun consumer。
- [ ] 1.4 新增或更新 semantic Cases，证明 entries不成为 Check/Record/dependency identities且 caller aggregation保持独立。

## Verification

- [ ] 2.1 运行 options/history/limits/messages/status/failure/fingerprint最窄 tests与 `bun run test-evidence -- check --root .`。
- [ ] 2.2 运行 product typecheck、lint、tests、docs/package candidate和 installed Bun acceptance。
- [ ] 2.3 运行 required/full Gate，复核没有 constructor、auto-advance、worktree delta、partial clean data或未声明 public surface。
