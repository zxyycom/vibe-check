# Proposal

本 Draft 保留 AGENTS.md 到项目文档的路由方案调整，待用户后续恢复；当前不实施路由重构，也不作为 0.0.2 发布前置。

## Why

发布准备期间曾向 AGENTS.md 增加分支与 worktree 路由，同时摘要了命名和发布规则。
这暴露出入口应承接多少任务判断、如何连接导航与领域 owner 的边界问题。
用户要求先撤回该行，保持 AGENTS.md 原样，将整体路由方案留待后续调整。

## Outcome

代理能从 AGENTS.md 按实际任务找到必要文档和验证入口，清楚区分入口摘要与完整规则 owner；
新增领域或调整文档位置时，有明确的路由维护位置，无需在多个入口重复维护同一规则。
